# Unix OS from Scratch -- Table of Contents and Status Log

This is the authoritative, unabridged per-chapter status log for this book (a fifth sibling to this author's CUDA C++ DSA, C++ Inference Engineering, Multi-GPU CUDA C++, and CUDA Hammer Tensor Compiler books). It grows one chapter at a time by explicit user confirmation before each new chapter starts, rather than being planned in full up front -- the user's own explicit choice when this book was scoped, 2026-09-24.

Local folder: `Documents/unix_os_from_scratch` (device-connected). GitHub remote: `https://github.com/MaximumBeings/unix_os_from_scratch.git`. mkdocs + Material theme, teal/deep-orange palette, `material/console` icon.

## Toolchain and machine status (confirmed 2026-09-24)

Cloud sandbox (x86_64, Ubuntu 24.04): real, working `nasm` 2.16.01, `gcc`/`ld` 13.3.0/2.42 (`-m32 -ffreestanding`, no separate cross-compiler needed since host and target architectures match), `grub-mkrescue`/`xorriso` 1.5.6 (GRUB 2.12), `qemu-system-x86_64` 8.2.2. Full pipeline (assemble, compile, link, pack into a bootable ISO, boot in QEMU, capture real serial output) proven working end to end before Chapter 1 was written.

Connected device (aarch64 macOS-hosted Linux VM): has `gcc` 11.4.0 and `grub-mkrescue`, but **no** `nasm`, **no** `qemu-system-x86_64`, and **no** root access to install either (`sudo -n true` fails with the same real "no new privileges" error this author's Hammer compiler book found in its own Appendix C). The gap is deeper than those two missing tools: the device's own `gcc` targets aarch64 and genuinely rejects `-m32` (`gcc: error: unrecognized command-line option '-m32'`, confirmed directly against File 001's own real `001_kmain.c`) -- an architecture-target mismatch, not a missing-package one, so even the freestanding C compile step cannot run there, not only assembly/linking/booting. This book's own bare-metal chapters are therefore genuinely built and genuinely *booted* on the cloud sandbox only -- a toolchain-availability limit confirmed directly, not assumed. Every source file is still sent to the device and md5-verified byte-identical, the same cross-machine discipline as every sibling book; the device simply cannot build or run any of it itself.

Git push note: this sandbox's own outbound git proxy only allows pushing to a pre-authorized set of repos for this session, and `unix_os_from_scratch` was not yet in that set as of this book's own first commit -- the user pushes this repo themselves (own credentials, own machine), same as every other book's own "hand off `git push` + `mkdocs build --strict`" pattern, just via a different path than the sandbox's own git remote for this one repo specifically.

## Part 1 -- Booting: From Power-On to Kernel Entry

Chapter 1 (DONE, commit 4e1097d): "Booting a Multiboot2 Kernel: From Power-On to Kernel Entry" -- the real boot chain from BIOS/UEFI firmware through GRUB to this book's own first C code, in four real steps; why this book does not write its own bootloader (real-mode setup and the protected-mode switch are GRUB's own already-correct, real work); the Multiboot2 header's own exact required fields (magic `0xE85250D6`, `header_length`, a checksum forcing a 32-bit sum of zero, a type-0/size-8 end tag), quoted directly from GNU's own Multiboot2 Specification (https://www.gnu.org/software/grub/manual/multiboot2/multiboot.html) and checked field-by-field against this chapter's own `001_boot.asm`. File 001 (`001_boot.asm`/`001_kmain.c`/`001_linker.ld`): a real Multiboot2-compliant entry stub that sets up a stack before calling into C (no stack exists yet when GRUB hands off control), a linker script placing the kernel at the conventional 1 MiB physical load address with `.multiboot_header` ordered first (spec requires it within the first 32 KiB), and a first `kmain()` that talks directly to the COM1 UART via raw `outb`/`inb` port I/O -- no `printf`, no OS, no heap. Real, honestly-kept `ld` warnings (`missing .note.GNU-stack section`, `LOAD segment with RWX permissions`) surfaced during the real build and are explained rather than silenced: this kernel has no paging yet, so there is no MMU-enforced permission system for those ELF-level flags to actually control regardless of what they claim -- a later chapter revisits this once real paging exists. Real, genuinely booted in QEMU 8.2.2 via a real GRUB-built ISO; QEMU's own `isa-debug-exit` device (`-device isa-debug-exit,iobase=0xf4,iosize=0x04`, written to via I/O port `0xf4`) gives a clean, deterministic exit (`QEMU real exit code: 1`) instead of an external process having to kill a kernel that halts forever -- this book's own first real output, captured via `-serial stdio` and locked byte-for-byte into the page (verified via a block-verifier script; 0 diagram-checker violations after 1 fix, a real shell heredoc `<<` in a bash command block that the checker's stray-`<` rule flagged, restructured to show the file content as its own block instead). 1 file (3 real source files: `.asm`/`.c`/`.ld`), cloud-sandbox-only per the toolchain-availability finding above; `mkdocs build --strict` clean.

## Part 2 -- Talking to Hardware: Early Device Drivers

Chapter 2 (DONE, commit 62fd74d): "VGA Text-Mode Output: A Real Screen Driver" -- the first chapter after booting, and the first in a new Part, since talking to hardware devices is a different concern from getting a kernel running at all. The VGA text-mode framebuffer at physical address `0xB8000` (real, cited: OSDev Wiki, "Text mode", https://wiki.osdev.org/Text_mode) is memory-mapped hardware -- an 80x25 grid of 2-byte cells, drawn with ordinary stores, no port I/O needed to draw a character. The attribute byte's real bit layout (foreground colour, a foreground-bright bit, background colour, a blink/bright-background bit) is quoted from the same OSDev page; moving the blinking hardware cursor is the one part of this driver that still needs real port I/O, through the VGA CRT Controller's index/data port pair (`0x3D4`/`0x3D5`, registers `0x0E`/`0x0F`), quoted and cross-checked against the OSDev Wiki's own example code ("Text Mode Cursor", https://wiki.osdev.org/Text_Mode_Cursor). File 002 (`002_boot.asm`/`002_linker.ld`, unchanged in shape from Chapter 1; `002_vga.h` -- this book's first header file; `002_vga.c`; `002_kmain.c`): a real VGA driver (`vga_init`/`vga_set_color`/`vga_putc`/`vga_puts`) plus a `kmain` that writes two lines in two different colours and then deliberately never exits (`qemu_exit` is not called), falling through to the boot stub's own halt loop so a QEMU monitor session connected from outside the running machine has time to inspect it. Real, genuinely booted in QEMU 8.2.2; verified two ways neither prior chapter needed: a live QEMU monitor session (`xp /64xb 0xb8000` for a real physical-memory hex dump, `screendump` for a real VGA screenshot, both captured via a Python script driving QEMU's Unix-socket monitor interface) and a real embedded screenshot (this book's first image asset), converted from QEMU's own `.ppm` output to PNG. The hex dump was decoded programmatically to settle a question the OSDev citation alone did not answer -- which byte of each 16-bit cell is the character and which is the attribute -- confirming the character byte sits at the lower address and the attribute byte matches `vga_entry_color(LIGHT_GREEN, BLACK) = 0x0a` exactly, real empirical verification standing in for a secondary citation that failed to fetch. A genuine `volatile` bug (`vga_buffer` declared as a plain, non-volatile pointer to memory-mapped I/O) was found and fixed before this page was locked -- caught by inspection, not by a failure, since it produced no warnings and no visibly wrong output at `-O0`; the QEMU monitor session and screenshot locked into the page are the post-fix, re-verified rebuild, not the original. 5 real source files (`.asm`/`.ld`/`.h`/`.c`/`.c`), cloud-sandbox-only per the toolchain-availability finding in Chapter 1; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

## Part 3 -- Kernel Infrastructure: Descriptor Tables and Utilities

Chapter 3 (DONE, commit 55d6c75): "A Real GDT and a Kernel printf" -- a new Part, since a CPU descriptor table and a software utility are a different concern from either booting or a hardware device driver. This kernel replaces GRUB's own transient GDT with three descriptors it owns and fully understands: a null descriptor, and flat 4 GiB kernel code/data segments. The segment-descriptor byte layout and the access-byte bit meanings (P, DPL, S, E, DC, RW, A) are quoted directly from the OSDev Wiki ("GDT Tutorial", "Global Descriptor Table"); this chapter's own access bytes (`0x9A` code, `0x92` data) and flags nibble (`0xC`) are derived bit-by-bit from that citation rather than pasted from memory. File 003 (`003_boot.asm`/`003_linker.ld`, unchanged in shape; `003_gdt.h`/`003_gdt.c`; `003_gdt_flush.asm` -- the real far-jump CS reload, cited and explained; `003_serial.h`/`003_serial.c` -- Chapter 2's own deferred refactor, the COM1 driver finally moved into a shared header; `003_printf.h`/`003_printf.c` -- this book's first genuinely reusable utility, a real variadic `kprintf` on freestanding C's own `<stdarg.h>` with no libc, supporting `%d`/`%u`/`%x`/`%c`/`%s`/`%%`; `003_vga.h`/`003_vga.c`, unchanged from Chapter 2). Real, genuinely booted in QEMU 8.2.2; verified two ways -- an exact serial-log capture of a real `kprintf` self-test call (every format specifier decoded correctly) and a real QEMU monitor screendump showing the same text on the actual VGA screen, wrapping correctly at column 80 exactly where the serial log's unwrapped text says it should. Reaching the first `kprintf` call at all -- after `gdt_init()` already ran and reloaded every segment register including a real far jump for CS -- is this chapter's own evidence that the GDT install did not triple-fault the machine. 6 real source files (2 new `.asm`, plus `.h`/`.c` pairs for gdt/serial/printf, `.h`/`.c` for vga unchanged), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 4 (DONE, commit 6cb1bf6): "Interrupts: The IDT and a First Real ISR" -- still Part 3, since an Interrupt Descriptor Table is the same kind of kernel infrastructure as Chapter 3's GDT. Installs a real 256-entry IDT (every unused vector deliberately zeroed -- Present bit clear -- rather than left uninitialized) with one real gate: vector 0, #DE Divide Error, type-attributes byte `0x8E` derived bit-by-bit from the OSDev Wiki's own cited worked example, the same discipline Chapter 3 used for its GDT access bytes. File 004 (`004_boot.asm`/`004_linker.ld`/`004_gdt.h`/`004_gdt.c`/`004_gdt_flush.asm`/`004_serial.h`/`004_serial.c`/`004_vga.h`/`004_vga.c`/`004_printf.h`/`004_printf.c`, unchanged from Chapter 3; `004_idt.h`/`004_idt.c` -- new; `004_idt_flush.asm` -- LIDT only, no far jump needed, unlike the GDT; `004_isr0.asm` -- a real hand-written ISR stub, `pusha`/call/`popa`/`iret`, explained field-by-field for why it cannot be an ordinary C function; `004_isr_handlers.h`/`004_isr_handlers.c` -- the real handler, which deliberately never returns since a divide-by-zero has no instruction to safely resume). This chapter's own evidence is a deliberately caused fault: `004_kmain.c` divides by a `volatile` runtime zero (not a compile-time constant the compiler could fold away) and the line immediately after it is captioned "THIS LINE SHOULD NEVER PRINT" -- verified two ways, an exact serial-log capture and a real QEMU monitor screendump, both confirming that line never appears, and confirming (via the machine staying alive and halted rather than triple-faulting, with `-no-reboot` passed specifically so a wrong gate would show up as an immediate QEMU exit rather than a silent reboot loop) that the fault dispatched correctly rather than crashing the CPU outright. 18 real source files total (7 new or changed this chapter: idt.h/.c, idt_flush.asm, isr0.asm, isr_handlers.h/.c, kmain.c; 11 carried forward unchanged from Chapter 3), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

## Part 4 -- Hardware Interrupts: Making the Kernel Interactive

Chapter 5 (DONE, commit 5518603): "PS/2 Keyboard Input, via a Real Hardware IRQ" -- a new Part, since a real external device driving this kernel asynchronously is a different concern from Part 3's own CPU-generated descriptor-table infrastructure. This is the first chapter in the book to enable interrupts globally (STI). Remaps both 8259 PICs off their real-mode-era default vectors (a real, cited, direct collision with the CPU's reserved exception range -- IRQ1 would otherwise fire on vector 0x09, the Coprocessor Segment Overrun exception) to 0x20/0x28, quoting the OSDev Wiki's own PIC_remap reference implementation structurally while deliberately stopping short of its final unmask-everything step (this kernel masks every line first, then unmasks only IRQ1, only after that line's own real IDT gate and handler already exist -- avoiding a timer-interrupt triple-fault the cited function's own ending would otherwise invite). File 005 (`005_boot.asm`/`005_linker.ld`/`005_gdt.h`/`005_gdt.c`/`005_gdt_flush.asm`/`005_idt_flush.asm`/`005_isr0.asm`/`005_isr_handlers.h`/`005_isr_handlers.c`/`005_serial.h`/`005_serial.c`/`005_vga.h`/`005_vga.c`/`005_printf.h`/`005_printf.c`, unchanged from Chapter 4; `005_idt.h`/`005_idt.c` -- adds one real gate, vector 0x21/IRQ1; `005_pic.h`/`005_pic.c` -- new; `005_irq1.asm` -- a real ISR stub, structurally identical to Chapter 4's isr0.asm; `005_keyboard.h`/`005_keyboard.c` -- new, a real Scan Code Set 1 lookup table cited directly from the OSDev Wiki, covering the alphabet/space/Enter only, a stated scope limit). This chapter's own verification is its most direct yet: real keystrokes injected from outside the running machine via QEMU's monitor `sendkey` command (`h e l l o <space> u n i x <enter>`), each one a genuine IRQ1 routed through the remapped PIC and this chapter's own IDT gate and scancode table, with the resulting text ("hello unix") captured byte-for-byte in both an exact serial log and a real VGA screenshot. 23 real source files total (8 new/changed this chapter: idt.h/.c, pic.h/.c, irq1.asm, keyboard.h/.c, kmain.c; 15 carried forward unchanged from Chapter 4), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 6 (DONE, commit ecc6491): "The PIT Timer and a Real Tick Counter" -- still Part 4, since the 8253/8254 Programmable Interval Timer is a second real hardware interrupt source alongside Chapter 5's keyboard, not a new architectural concern. Programs PIT channel 0 for Mode 3 (square wave generator) at 100 Hz via I/O ports 0x40 (channel 0 data) and 0x43 (mode/command register), with every bit of the command byte (`0x36`) derived from the OSDev Wiki's own cited bit layout (channel select, access mode, operating mode, BCD/binary) rather than pasted, and the divisor (`1193182 / 100 = 11931`) computed from the chip's own cited ~1.193182 MHz base oscillator frequency. File 006 (`006_boot.asm`/`006_linker.ld`/`006_gdt.h`/`006_gdt.c`/`006_gdt_flush.asm`/`006_idt_flush.asm`/`006_isr0.asm`/`006_isr_handlers.h`/`006_isr_handlers.c`/`006_irq1.asm`/`006_keyboard.h`/`006_keyboard.c`/`006_pic.h`/`006_pic.c`/`006_serial.h`/`006_serial.c`/`006_vga.h`/`006_vga.c`/`006_printf.h`/`006_printf.c`, unchanged from Chapter 5; `006_idt.h`/`006_idt.c` -- adds one real gate, vector 0x20/IRQ0; `006_pit.h`/`006_pit.c` -- new; `006_irq0.asm` -- a real ISR stub, structurally identical to Chapter 5's irq1.asm; `006_kmain.c` -- unmasks both IRQ0 and IRQ1 in a still-ordering-sensitive sequence). This chapter's own verification went beyond reading output: a second process connected to the same QEMU monitor socket recorded real host wall-clock timestamps around two independent 5-second sleeps and diffed the kernel's own tick counter in between, measuring ~500 ticks against ~5.000-5.001 real seconds both times (confirming the programmed 100 Hz rate empirically, not just by inspection) -- and, between those windows, injected two real keystrokes (`sendkey h`, `sendkey i`) that landed correctly mid-tick-stream in the real serial capture (`hitick: 1800`), regression-proving Chapter 5's IRQ1 path still works with a second real interrupt source now active. 26 real source files total (6 new/changed this chapter: idt.h/.c, pit.h/.c, irq0.asm, kmain.c; 20 carried forward unchanged from Chapter 5), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

## Part 5 -- Memory: Discovering and Managing Real RAM

Chapter 7 (DONE, commit 5ed761f): "Physical Memory: the Multiboot2 Memory Map and a Real Frame Allocator" -- a new Part, since discovering and managing physical memory is a different concern from either descriptor-table infrastructure (Part 3) or hardware interrupts (Part 4). The first chapter to read GRUB's own real machine-state handoff beyond the boot sequence itself: EAX holds the real Multiboot2 magic value `0x36d76289` and EBX the physical address of the boot information structure (GNU Multiboot2 Specification, "Machine state"), both captured by `007_boot.asm` and passed into `kmain` as real cdecl arguments for the first time in this book. Parses the real memory map (tag type 6) using the spec's own cited struct layout (`multiboot_tag_mmap`/`multiboot_mmap_entry`, fields quoted verbatim from the spec's own example code), walking tags by their own reported, 8-byte-rounded size rather than assuming any fixed layout. File 007 (`007_gdt.h`/`007_gdt.c`/`007_gdt_flush.asm`/`007_idt.h`/`007_idt.c`/`007_idt_flush.asm`/`007_isr0.asm`/`007_isr_handlers.h`/`007_isr_handlers.c`/`007_irq0.asm`/`007_irq1.asm`/`007_keyboard.h`/`007_keyboard.c`/`007_pic.h`/`007_pic.c`/`007_pit.h`/`007_pit.c`/`007_serial.h`/`007_serial.c`/`007_vga.h`/`007_vga.c`, unchanged from Chapter 6; `007_boot.asm`/`007_linker.ld` -- changed, now pass EAX/EBX into kmain and export a real `kernel_end` linker symbol; `007_multiboot.h`/`007_multiboot.c` -- new; `007_pmm.h`/`007_pmm.c` -- new, a real bit-per-frame bitmap allocator up to a 64 MiB ceiling matching this chapter's own QEMU `-m 64` run, seeded from real "available" memory-map regions with the kernel's own image carved out last via the linker's `kernel_end`; `007_printf.h`/`007_printf.c` -- changed, adds a real `%llx` 64-bit hex conversion; `007_kmain.c` -- changed). A genuine build failure surfaced and was fixed honestly: the first real build of `007_printf.c` failed with `undefined reference to '__umoddi3'`/`__udivdi3'` (a 64-bit division on this 32-bit, no-libgcc freestanding target), fixed by rewriting the 64-bit hex printer to use only shifts/masks (`& 0xF`, `>>= 4`) instead of division -- avoiding the dependency rather than linking libgcc to satisfy it. This chapter's own verification went further than reading output: the real free-frame count QEMU's real memory map should produce (16089, after clamping the low region below 1 MiB and the high region to the 64 MiB ceiling, then subtracting the kernel's own 7 real frames) was computed independently and matched the kernel's own live-printed count exactly, and the allocate-three/free-the-middle/allocate-again sequence was confirmed, not assumed, to reuse the exact freed address. 30 real source files total (9 new/changed this chapter: boot.asm, linker.ld, multiboot.h/.c, pmm.h/.c, printf.h/.c, kmain.c; 21 carried forward unchanged from Chapter 6), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 8 (DONE, commit 4d17cdc): "Paging: Enabling Virtual Memory" -- stays in Part 5, since enabling address translation is still fundamentally about managing this machine's real memory, one layer above the physical frames Chapter 7 already tracks. Builds a real two-level x86 (non-PAE) page directory/page table structure cited field-for-field from the OSDev Wiki ("Paging": PDE/PTE bit layout -- Present bit 0, R/W bit 1, U/S bit 2, physical address bits 12-31 4KiB-aligned; 1024 4-byte entries per table, one page table covers 4 MiB; CR3 holds the page directory's physical address; CR0 bit 31 (PG) switches paging on, cited example `or eax, 0x80000001` setting PG+PE together). File 008 (`008_gdt.h`/`008_gdt.c`/`008_gdt_flush.asm`/`008_idt.h`/`008_idt.c`/`008_idt_flush.asm`/`008_isr0.asm`/`008_isr_handlers.h`/`008_isr_handlers.c`/`008_irq0.asm`/`008_irq1.asm`/`008_keyboard.h`/`008_keyboard.c`/`008_pic.h`/`008_pic.c`/`008_pit.h`/`008_pit.c`/`008_serial.h`/`008_serial.c`/`008_vga.h`/`008_vga.c`/`008_boot.asm`/`008_linker.ld`/`008_multiboot.h`/`008_multiboot.c`/`008_pmm.h`/`008_pmm.c`/`008_printf.h`/`008_printf.c`, unchanged from Chapter 7; `008_paging.h`/`008_paging.c` -- new, identity-maps the same 0-64 MiB range Chapter 7's own PMM manages using 17 real frames pulled from `pmm_alloc_frame()` (one page directory + 16 page tables) rather than static arrays, then loads CR3 and sets CR0.PG via inline asm; `008_kmain.c` -- changed, calls `paging_init()` then maps a fresh frame at virtual address `0xC0000000` -- deliberately outside the identity-mapped range, forcing `paging_map_page` to allocate a genuinely new page table -- and proves translation is real by writing `0xCAFEF00D` through that virtual address and reading the same value back through the same physical frame's identity-mapped address). This chapter's own verification: the free-frame count (16088, after the kernel's own slightly larger 8-frame footprint this chapter's added code produces) was recomputed independently from this run's real memory map and matched the kernel's own live-printed count exactly, the same cross-check discipline Chapter 7 established; CR0 was read back after setting PG and printed (`0x80000011`, bit 31 confirmed set) rather than trusted blindly; and the virtual/physical aliasing test's two reads were shown to match in a real, live-executed QEMU run, with interrupts/PIT/keyboard confirmed still functioning normally afterward. 32 real source files total (2 new this chapter: paging.h/.c; kmain.c changed; 29 carried forward unchanged from Chapter 7), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 9 (DONE, commit c27ce2d): "A Kernel Heap: Real Dynamic Memory with kmalloc/kfree" -- stays in Part 5, the third and final layer above Chapter 7's physical frames for now: a real free-list heap allocator, in the shape the OSDev Wiki itself describes ("Memory Allocation": hidden headers just before each returned block, a free-zone descriptor with next/prev pointers and a size, kept sorted by address to enable merging; "Writing a memory manager": splitting a block when the leftover is large enough, coalescing adjacent free blocks on free). File 009 (`009_gdt.h`/`009_gdt.c`/`009_gdt_flush.asm`/`009_idt.h`/`009_idt.c`/`009_idt_flush.asm`/`009_isr0.asm`/`009_isr_handlers.h`/`009_isr_handlers.c`/`009_irq0.asm`/`009_irq1.asm`/`009_keyboard.h`/`009_keyboard.c`/`009_pic.h`/`009_pic.c`/`009_pit.h`/`009_pit.c`/`009_serial.h`/`009_serial.c`/`009_vga.h`/`009_vga.c`/`009_boot.asm`/`009_linker.ld`/`009_multiboot.h`/`009_multiboot.c`/`009_pmm.h`/`009_pmm.c`/`009_printf.h`/`009_printf.c`/`009_paging.h`/`009_paging.c`, unchanged from Chapter 8; `009_kheap.h`/`009_kheap.c` -- new, a real `kmalloc`/`kfree` built entirely from `pmm_alloc_frame()` (Chapter 7) and `paging_map_page()` (Chapter 8): a dedicated virtual heap region at `0xD0000000`, mapped 4 pages at a time on demand, with first-fit search, splitting above a minimum useful leftover, and immediate forward-then-backward coalescing on free; `009_kmain.c` -- changed, keeps Chapters 7-8's own demonstrations and adds a real kheap demonstration using a new `kheap_dump()` verification helper). This chapter's own verification: the free-frame count (16088) and every heap byte count -- the initial 16368 usable bytes, the 16096-byte leftover after three real allocations, the exact 16368-byte figure the free list returns to after three frees fully coalesce it back to one block, and the 5-page/20480-byte growth triggered by a 20000-byte allocation -- were each recomputed independently and matched the kernel's own live output exactly; block reuse after `kfree` was confirmed (not assumed) via the kernel's own `%s` check, the same discipline Chapter 7 established. 33 real source files total (2 new this chapter: kheap.h/.c; kmain.c changed; 31 carried forward unchanged from Chapter 8), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 10 (DONE, commit 13ea698): "Page Fault Handling: A Real ISR14" -- closes out Part 5, the memory story this Part has built since Chapter 7: a real handler for vector 14 (#PF), the one CPU exception paging has been able to raise since Chapter 8 with nothing installed to catch it. Cited field-for-field from the OSDev Wiki ("Exceptions": error code bits P/W/U -- present-vs-non-present, read-vs-write, supervisor-vs-user -- and "it sets the value of the CR2 register to the virtual address which caused the Page Fault"). File 010 (`010_gdt.h`/`010_gdt.c`/`010_gdt_flush.asm`/`010_idt_flush.asm`/`010_isr0.asm`/`010_irq0.asm`/`010_irq1.asm`/`010_keyboard.h`/`010_keyboard.c`/`010_pic.h`/`010_pic.c`/`010_pit.h`/`010_pit.c`/`010_serial.h`/`010_serial.c`/`010_vga.h`/`010_vga.c`/`010_boot.asm`/`010_linker.ld`/`010_multiboot.h`/`010_multiboot.c`/`010_pmm.h`/`010_pmm.c`/`010_printf.h`/`010_printf.c`/`010_paging.h`/`010_paging.c`/`010_kheap.h`/`010_kheap.c`, unchanged from Chapter 9; `010_isr14.asm` -- new, a real ISR stub structurally different from vector 0's (reads the CPU's real error code off the stack at `[esp+32]`, passes it as a cdecl argument, discards it with `add esp, 4` before IRET since IRET never expects it); `010_isr_handlers.h`/`010_isr_handlers.c` -- changed, adds `isr14_handler` reading CR2 via inline asm and decoding the error code bit-by-bit; `010_idt.h`/`010_idt.c` -- changed, installs the vector-14 gate; `010_kmain.c` -- changed, keeps Chapters 7-9's own demonstrations, waits for 200 real PIT ticks to prove interrupts still work, then deliberately writes through an unmapped virtual address (`0xE0000000`, page-directory index 896, chosen to fall outside every range any earlier chapter ever mapped) to trigger a genuine #PF). This chapter's own verification: the real fault's CR2 readback (`0xE0000000`) matched the deliberate target exactly, and the real error code (`0x2`) decoded correctly as non-present/write/supervisor -- each bit independently checkable against what the trigger code actually did; the handler's own halt was confirmed by the trigger's own follow-up print line never appearing in the real capture. Free-frame count (16088) cross-checked independently, same discipline as Chapters 7-9. 35 real source files total (1 new: isr14.asm; 4 changed: isr_handlers.h/.c, idt.h/.c, kmain.c; 29 carried forward unchanged from Chapter 9), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

## Part 6 -- Scheduling: Running More Than One Task at Once

Chapter 11 (DONE, commit 64d7938): "Kernel-Level Multitasking: Cooperative Task Switching" -- a new Part, the first chapter where more than one real flow of control exists in this kernel at once. A real context switch (`switch_task`) built directly on this book's own cdecl calling convention (established since Chapter 3): cited from the OSDev Wiki ("Kernel Multitasking": "For cdecl; EAX, ECX, and EDX are already saved by the caller and don't need to be saved again"), meaning only EBX/ESI/EDI/EBP need saving by hand, with EIP handled structurally by `call`/`ret` rather than as saved data. A brand-new task's very first stack frame is built by hand to match exactly what `switch_task`'s own epilogue expects to pop -- cited directly ("Kernel Multitasking": "the kernel needs to put values on the new task's kernel stack to match the values that 'switch_tasks' expects to pop off"), with the OSDev Wiki's own fuller "X86 Cooperative Multitasking Tutorial" (a full saved-registers struct, `task->regs.esp`/`task->regs.eip` set directly) cited and explicitly discussed as the more complete real alternative this chapter deliberately narrows down to fit this book's cdecl-only C convention. File 011 (`011_gdt.h`/`011_gdt.c`/`011_gdt_flush.asm`/`011_idt.h`/`011_idt.c`/`011_idt_flush.asm`/`011_isr0.asm`/`011_isr14.asm`/`011_isr_handlers.h`/`011_isr_handlers.c`/`011_irq0.asm`/`011_irq1.asm`/`011_keyboard.h`/`011_keyboard.c`/`011_pic.h`/`011_pic.c`/`011_pit.h`/`011_pit.c`/`011_serial.h`/`011_serial.c`/`011_vga.h`/`011_vga.c`/`011_boot.asm`/`011_linker.ld`/`011_multiboot.h`/`011_multiboot.c`/`011_pmm.h`/`011_pmm.c`/`011_printf.h`/`011_printf.c`/`011_paging.h`/`011_paging.c`/`011_kheap.h`/`011_kheap.c`, unchanged from Chapter 10 (Chapter 10's own vector-14 #PF gate stays installed but is deliberately never triggered this chapter, since that fault halts the kernel for good and this chapter needs execution to continue); `011_switch.asm` -- new, the real context-switch routine; `011_task.h`/`011_task.c` -- new, a fixed-size TCB table, `task_create`/`task_yield`/`task_exit`, and a round-robin scheduler that skips tasks marked done; `011_kmain.c` -- changed, keeps Chapters 7-10's own demonstrations (minus Chapter 10's own deliberate-fault trigger specifically) and adds two real cooperative tasks, each `kmalloc()`-backed, printing three interleaved messages before calling `task_exit()`). This chapter's own verification: the real serial capture shows Task A's and Task B's messages genuinely interleaved (not run sequentially), and the kernel's own printed context-switch count (12) was independently derived by counting every real `task_yield()` call this run made (4 from Task A, 4 from Task B, 4 from kmain's own wait loop) rather than by tracing the full switch order, matching exactly. Free-frame count (16087, one fewer than Chapter 10's 16088 since this chapter's own new code makes the kernel image one frame larger) cross-checked independently, same discipline as Chapters 7-10. 38 real source files total (3 new: switch.asm, task.h/.c; kmain.c changed; 34 carried forward unchanged from Chapter 10), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 12 (DONE, commit 683d3c1): "Preemptive Multitasking: Letting the Timer Decide" -- stays in Part 6, upgrading Chapter 11's cooperative scheduler to real preemption. Adds exactly one new call, `task_tick()`, to `012_pit.c`'s own `irq0_handler` -- placed deliberately after `pic_send_eoi(0)`, reasoned through explicitly in the chapter text: switching away before EOI could stall the PIC into withholding future ticks from every other task, since the one thing that could reschedule the stalled task to finally send that EOI is itself a tick that can no longer fire. Surfaces and fixes a real CPU-state subtlety cooperative scheduling never faced: `switch_task()` (unchanged since Chapter 11, `012_switch.asm` needed zero edits) never saves or restores EFLAGS, so IF -- the CPU's one shared interrupt-enable flag, cleared automatically on entry to this book's own interrupt-gate-type IDT gates (`0x8E`, since Chapter 4) -- silently carries into whichever task is switched into next. Cited directly (OSDev Wiki, "Kernel Multitasking": "Caller is expected to disable IRQs before calling, and enable IRQs again after function returns"), `task_yield()` now brackets every switch with `cli`/`sti` itself, and a new `task_start_trampoline()` gives a brand-new task's first-ever run the identical guarantee (a task's real `entry` function is no longer written directly onto its initial stack frame; the trampoline is, and it looks the entry function up from a new `entry` field on the TCB and calls `sti` before running it, with a defensive fallback call to `task_exit()` if the entry function ever returns instead of calling it itself). File 012 (`012_gdt.h`/`012_gdt.c`/`012_gdt_flush.asm`/`012_idt.h`/`012_idt.c`/`012_idt_flush.asm`/`012_isr0.asm`/`012_isr14.asm`/`012_isr_handlers.h`/`012_isr_handlers.c`/`012_irq1.asm`/`012_keyboard.h`/`012_keyboard.c`/`012_pic.h`/`012_pic.c`/`012_serial.h`/`012_serial.c`/`012_vga.h`/`012_vga.c`/`012_boot.asm`/`012_linker.ld`/`012_multiboot.h`/`012_multiboot.c`/`012_pmm.h`/`012_pmm.c`/`012_printf.h`/`012_printf.c`/`012_paging.h`/`012_paging.c`/`012_kheap.h`/`012_kheap.c`/`012_switch.asm`, unchanged from Chapter 11; `012_pit.h`/`012_pit.c` -- changed, adds the `task_tick()` call site; `012_task.h`/`012_task.c` -- changed, adds the trampoline, the cli/sti bracketing, and `task_tick()`; `012_irq0.asm` -- unchanged in content, carried forward under its own name; `012_kmain.c` -- changed, two real tasks that contain no `task_yield()` call anywhere in their own code, relying entirely on the real timer to interleave them). This chapter's own verification: the real interleaving itself (Task A and Task B's own progress checkpoints appear in genuinely mixed chunks, proving the timer alone decided the split points), plus a real, checkable formula derived and confirmed across three independent real runs rather than one -- switch count equals real ticks elapsed (while both tasks are still unfinished) plus 2 (one exit-driven switch per task, triggered directly by each task's own `task_exit()` call rather than by any tick) -- which held exactly in all three (9+2=11, 12+2=14, 7+2=9), despite the real tick count itself genuinely not being reproducible run to run in this emulated environment, an honestly-reported real characteristic rather than a hidden inconsistency. Free-frame count (16087) unchanged from Chapter 11, same discipline as Chapters 7-11. 38 real source files total (0 new files this chapter -- a first for this book; 5 changed: pit.h/.c, task.h/.c, kmain.c; 33 carried forward unchanged from Chapter 11 including `012_switch.asm`, which needed no edits despite this chapter's own real IF-handling change), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 13 (DONE, commit 7bc0821): "Synchronization Primitives: Spinlocks" -- stays in Part 6, protecting Chapter 9's free-list `kmalloc()`/`kfree()` (unchanged in algorithm since then) from Chapter 12's own preemptive scheduler, which never asked what real concurrent access does to that shared free list. Captures the real bug before fixing it: two new demo tasks, Stress A and Stress B, hammer `kmalloc()`/`kfree()` (3,000,000 round trips each) with no synchronization at all, against the exact unprotected Chapter-9-era code; one real captured run shows the free list corrupted into a genuine self-referential cycle (`block->next` pointing back at itself, `size` underflowed to `0xFFFFFFF0`), making `kheap_dump()`'s own unbounded `while` loop print the same corrupted block forever until the run was killed externally; a separate real repeat run of the identical unprotected code, under a different QEMU RAM size, instead produced a genuine page fault (`*** CPU EXCEPTION: Page Fault`) from dereferencing corrupted pointer garbage -- two different real crashes from the same unguarded race, captured as real evidence rather than merely asserted. Fixes it with a new `013_spinlock.h`/`013_spinlock.c`: a `cli`/`sti`-based critical section that saves the caller's real EFLAGS via `pushf`/`pop` before disabling interrupts and restores exactly that value via `push`/`popf` on release (paralleling Linux's own `spin_lock_irqsave`/`spin_unlock_irqrestore`, presented explicitly as this book's own reasoned engineering design, not a wiki quote), deliberately not using the atomic test-and-set/`lock bts`/`lock btr` instructions the OSDev Wiki's own "Spinlock" and "Synchronization Primitives" pages describe for real multiprocessor hardware -- both pages fetched and cited directly, with the chapter explicitly reasoning why this specific single-CPU kernel (Chapter 12 already established its only preemption source is the maskable timer interrupt) does not need them. `013_kheap.c`'s own free-list algorithm is unchanged; `kmalloc()`, `kfree()`, and `kheap_dump()` each now hold `kheap_lock` for their full duration (`kheap_expand()` takes no lock of its own, since it is only ever called from inside `kmalloc()`'s already-locked critical section). File 013 (`013_gdt.h`/`013_gdt.c`/`013_gdt_flush.asm`/`013_idt.h`/`013_idt.c`/`013_idt_flush.asm`/`013_isr0.asm`/`013_isr14.asm`/`013_isr_handlers.h`/`013_isr_handlers.c`/`013_irq0.asm`/`013_irq1.asm`/`013_keyboard.h`/`013_keyboard.c`/`013_pic.h`/`013_pic.c`/`013_pit.h`/`013_pit.c`/`013_serial.h`/`013_serial.c`/`013_vga.h`/`013_vga.c`/`013_boot.asm`/`013_linker.ld`/`013_multiboot.h`/`013_multiboot.c`/`013_pmm.h`/`013_pmm.c`/`013_printf.h`/`013_printf.c`/`013_paging.h`/`013_paging.c`/`013_switch.asm`/`013_task.h`, unchanged from Chapter 12; `013_task.c` -- changed, `TASK_MAX_TASKS` 3 -> 5 for the two new stress tasks; `013_kheap.c` -- changed, the spinlock integration described above; `013_kmain.c` -- changed, adds Stress A/Stress B and pre/post `kheap_dump()` calls after Chapter 12's own Task A/Task B demo, which is otherwise carried forward unchanged; `013_spinlock.h`/`013_spinlock.c` -- new). This chapter's own verification: the real "before" capture (a genuine cyclic free list, quoted byte-for-byte up through several repeated cycle lines) and the real "after" capture (clean completion, 221 total real context switches, `kheap_dump()` showing exactly four 4096-byte USED task-stack blocks plus one clean FREE block) from the identical stress-test code, differing only in whether `013_kheap.c` held the new lock. Free-frame count (16087) unchanged from Chapters 11-12, same discipline as every chapter since 7. 40 real source files total (2 new: spinlock.h/.c; 3 changed: kheap.c, task.c, kmain.c; 35 carried forward unchanged from Chapter 12), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 14 (DONE, commit 6f0220d): "Blocking Synchronization: Semaphores and Sleep/Wake" -- stays in Part 6, replacing Chapter 13's own spinlocks with a real blocking primitive for the one thing spinlocks are the wrong tool for: waits that can be long and unpredictable, where spinning would waste real CPU time a genuinely idle task should give up entirely. Grounded in two real OSDev Wiki citations ("Semaphore": "it is an opaque data type with two defined operations, usually called wait and signal", and "a thread calling wait on a semaphore whose value is 0 should be removed from the scheduler queue, and only added to it again when the semaphore is signalled"; "Synchronization Primitives": "the function p...decrements the value of the semaphore, and if the semaphore is negative, puts the process on the waiting queue"). Deliberately chose a simpler non-negative-count-plus-explicit-waiter-queue convention over the "count goes negative" convention some texts describe, reasoned through explicitly in the chapter text as this book's own engineering choice, not a wiki quote -- still matches the cited "wake the thread at the front of the queue" behavior. Built entirely on top of Chapter 13's own spinlock (`014_semaphore.c`'s `count` and FIFO waiter queue are protected by a `spinlock_t`, not by anything new). Identifies and fixes, through careful design-time reasoning rather than a failed run, a real and classic lost-wakeup race: a naive `semaphore_wait()` that enqueues a waiter, releases the lock, and only then calls a combined "block and yield" helper leaves a real gap in which a concurrent `semaphore_signal()` could dequeue and "wake" (a harmless no-op) a still-READY task, believing the handoff succeeded, right before that task blocks itself anyway and sleeps forever. Fixed by splitting the blocking primitive into a new `task_block_self()` (`014_task.h`/`014_task.c`, sets state only, does not yield) called together with `enqueue_waiter()` while still holding the semaphore's own lock (interrupts still off), with the lock release and the actual `task_yield()` call happening only afterward. Expands the task state model from Chapters 11-13's single `done` boolean to a real three-state `task_state_t` enum (READY/BLOCKED/DONE), which exposes and fixes a genuine bug in `task_yield()`'s own round-robin scan carried forward unnoticed since Chapter 11: initializing `next = old` silently assumed the calling task was always a valid READY fallback, true only as long as no task could make itself non-runnable mid-yield -- fixed by initializing `next = -1` and only ever setting it inside the scan loop, so the loop's own wraparound still correctly finds `old` when it genuinely is still READY, and correctly reports "no runnable task remains" only when even `old` is not. Demonstrated with a real bounded-buffer producer/consumer (`014_kmain.c`: 2 producers, 2 consumers, a 4-slot shared buffer deliberately small relative to 15 items per task to force real blocking both ways, `sem_empty_slots`/`sem_full_slots` counting the buffer's free/filled slots, a separate `spinlock_t buffer_lock` guarding only the short index-update critical section). Verified not by reading the code but by programmatically extracting every real `produced N`/`consumed N` value from the captured serial log and diffing the two sets -- confirmed an exact 1:1 match across all 30 items (15 from each producer), no losses and no duplicates, presented in the chapter as a stronger form of verification than trusting the code by eye. File 014 (`014_gdt.h`/`014_gdt.c`/`014_gdt_flush.asm`/`014_idt.h`/`014_idt.c`/`014_idt_flush.asm`/`014_isr0.asm`/`014_isr14.asm`/`014_isr_handlers.h`/`014_isr_handlers.c`/`014_irq0.asm`/`014_irq1.asm`/`014_keyboard.h`/`014_keyboard.c`/`014_pic.h`/`014_pic.c`/`014_pit.h`/`014_pit.c`/`014_serial.h`/`014_serial.c`/`014_vga.h`/`014_vga.c`/`014_boot.asm`/`014_linker.ld`/`014_multiboot.h`/`014_multiboot.c`/`014_pmm.h`/`014_pmm.c`/`014_printf.h`/`014_printf.c`/`014_paging.h`/`014_paging.c`/`014_switch.asm`/`014_kheap.h`/`014_kheap.c`/`014_spinlock.h`/`014_spinlock.c`, unchanged from Chapter 13; `014_task.h`/`014_task.c` -- changed, the three-state enum, `task_block_self()`/`task_wake()`/`task_current_id()`, the `next=-1` scheduler fix, `TASK_MAX_TASKS` 5 -> 9; `014_kmain.c` -- changed, adds the producer/consumer demo after Chapter 13's own stress-test demo, which is otherwise carried forward unchanged; `014_semaphore.h`/`014_semaphore.c` -- new). This chapter's own real free-frame count is 16086 (one frame less than every chapter since 7's own 16087), an honestly-reported real consequence of this chapter's own larger kernel image, not an inconsistency to paper over. 251 total real context switches in the official captured run. 42 real source files total (2 new: semaphore.h/.c; 2 changed: task.h/.c, kmain.c; 38 carried forward unchanged from Chapter 13), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 15 (DONE, commit b3a5e53): "User Mode: Ring 3, a TSS, and a First System Call" -- opens a new Part 7 ("Privilege: Running Code This Kernel Does Not Fully Trust"), moving past Part 6's scheduling arc to this kernel's first real privilege boundary: every task since Chapter 1 has run at ring 0 with full hardware privilege, and this chapter gives it a genuine, CPU-enforced way to run something at ring 3 instead. Grounded in real OSDev Wiki citations ("Getting to Ring 3": "Add 2 new segments to your GDT... with a base of 0, a limit of 0xFFFFFFFF, and a privilege level of 3" and "it is impossible to return to ring 0 for system calls, faults, or even IRQs" without a TSS; "Task State Segment" for the real ESP0/SS0 field semantics; "System Calls" for the INT-based syscall convention; "Exceptions" for #GP's own error-code semantics). Builds a real TSS (`015_tss.h`/`015_tss.c`) used purely so the CPU has a real kernel stack to find on any ring-3-raised interrupt -- explicitly NOT for the CPU's own hardware task-switching, which this kernel never uses; extends the GDT from 3 to 6 descriptors (`015_gdt.c`: ring-3 code/data plus the TSS descriptor); adds a real ring 0->3 transition (`015_usermode.asm`, a hand-built `iret` stack, cited directly from "Getting to Ring 3"'s own "make the processor think it was already in ring 3 to start with"); and adds this book's first syscall (`015_isr128.asm`, INT 0x80, DPL=3 -- the one gate in this whole book ring-3 code is allowed to invoke directly), dispatched by a new `isr128_handler()` that lets ring-3 code print via `kprintf()` despite having no port-I/O access at all (`015_tss.c`'s own `iomap_base` trick). Catches a real bug BEFORE fixing it, the same style as Chapter 13: the first real attempt at granting `PAGE_USER` (`015_paging.h`, a new page-table/page-directory bit) only set it on the page-TABLE entry, producing a genuine captured `#PF` (CR2 = the demo function's own address, error code 0x5) the instant ring 3 tried to fetch its first instruction -- traced to the real x86 rule that paging checks the U/S bit of BOTH the PDE and the PTE, using whichever is more restrictive, and every identity-mapped PDE was built supervisor-only back in Chapter 8. Fixed by ORing `PAGE_USER` into the PDE too, inside `paging_map_page()` itself. A dedicated, page-aligned `.usermode_text` linker section (`015_linker.ld`) marks EXACTLY the one demo function's own page as ring-3-executable -- confirmed via `nm` landing precisely at its own page boundary -- leaving every other line of this kernel, including every driver, still ring-0-only. This chapter's own real proof that ring 3 is genuinely enforced, not merely claimed: the demo function deliberately executes `CLI` (a privileged instruction) after printing its syscall-routed message, and the real captured run shows a genuine `#GP` (error code 0x0, correctly decoded by the new `isr13_handler()` as non-segment-related) rather than success. File 015 (`015_boot.asm`/`015_gdt_flush.asm`/`015_idt_flush.asm`/`015_isr0.asm`/`015_isr14.asm`/`015_irq0.asm`/`015_irq1.asm`/`015_keyboard.h`/`015_keyboard.c`/`015_kheap.h`/`015_kheap.c`/`015_multiboot.h`/`015_multiboot.c`/`015_pic.h`/`015_pic.c`/`015_pit.h`/`015_pit.c`/`015_pmm.h`/`015_pmm.c`/`015_printf.h`/`015_printf.c`/`015_semaphore.h`/`015_semaphore.c`/`015_serial.h`/`015_serial.c`/`015_spinlock.h`/`015_spinlock.c`/`015_switch.asm`/`015_task.h`/`015_task.c`/`015_vga.h`/`015_vga.c`, unchanged from Chapter 14; `015_gdt.h`/`015_gdt.c`/`015_idt.h`/`015_idt.c`/`015_isr_handlers.h`/`015_isr_handlers.c`/`015_paging.h`/`015_paging.c`/`015_linker.ld`/`015_kmain.c` -- changed; `015_tss.h`/`015_tss.c`/`015_usermode.asm`/`015_isr13.asm`/`015_isr128.asm`/`015_syscall.h` -- new). This chapter's own real free-frame count is 16084 (two frames lower than Chapter 14's 16086), an honestly-reported consequence of this chapter's own larger kernel image. 48 real source files total (6 new, 10 changed, 32 carried forward unchanged from Chapter 14), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 16 (DONE, commit 0452467): "Real Ring-3 Tasks in the Scheduler: sys_yield, sys_exit, Multiple User Tasks" -- stays in Part 7, turning Chapter 15's one-off ring-3 demo (wired up entirely by hand inside `kmain()`, outside Chapter 11's own task table) into a real, general capability of this kernel's own scheduler. Grounded in a fresh, targeted OSDev Wiki citation ("Kernel Multitasking": "Adjust the ESP0 field in the TSS (used by CPU for CPL=3 -> CPL=0 privilege level changes)", from that page's own 32-bit context-switch example) confirming that per-task TSS.ESP0 switching -- not just a single shared kernel stack, as Chapter 15's one-task demo could get away with -- is a real, necessary technique the instant a second ring-3 task exists. New `task_create_usermode()` (`016_task.h`/`016_task.c`) builds each ring-3 task a real, dedicated kernel stack (kmalloc()'d, pre-populated exactly like an ordinary task's own switch_task() frame) that doubles as that task's own TSS.ESP0 target, plus a real PAGE_USER user stack, and marks `016_linker.ld`'s own `.usermode_text` pages user-accessible once, no matter how many ring-3 tasks are ever created (moved here from Chapter 15's own `kmain()`, which did this by hand for its single demo task). `task_start_trampoline()` now branches on a new `is_usermode` TCB field, calling `enter_usermode()` instead of `entry()` directly for a ring-3 task's very first activation; `task_yield()` now reloads the TSS's ESP0 field, via `tss_set_kernel_stack()`, immediately before every single switch INTO a ring-3 task -- covering both first activation and every later resume identically -- and deliberately skips this entirely for ring-0-only tasks, reasoned through explicitly in the chapter text as safe because TSS.ESP0 is only ever consulted at a genuine CPL 3->0 transition. Two new syscalls, `SYS_YIELD` and `SYS_EXIT` (`016_syscall.h`), needed zero new scheduling logic: `016_isr_handlers.c`'s `isr128_handler()` becomes a real `switch` statement whose two new cases are direct, un-special-cased calls into `task_yield()`/`task_exit()`, the same functions this book has had since Chapter 11/12, genuinely unaware they were reached through a ring-3 syscall rather than an ordinary ring-0 call site. Demonstrated with two real ring-3 tasks (`016_kmain.c`'s `ring3_task_a_entry()`/`ring3_task_b_entry()`, sharing a small `usermode_syscall()` wrapper), each printing and then genuinely `SYS_YIELD`ing 5 times before a real `SYS_EXIT` -- the real captured run shows perfect A/B alternation across all 10 prints and, for the very first time in this book, `kmain()` itself falling off the end of its own function body and landing in `016_boot.asm`'s own `cli`/`hlt` hang loop, since no ring-3 code is left for it to wait on or transition into. Verified with a real, independently-checkable LOWER bound on this phase's own switch count (2 tasks * 5 SYS_YIELD + 2 SYS_EXIT-triggered switches = 12), reported honestly alongside the real excess (6 more, from real IRQ0 tick preemption landing in this narrow window during the official captured run -- a fact about that exact run's own wall-clock timing under QEMU, not a fixed number, the same deliberate nondeterminism Chapter 12 first established and this book has never hidden since). File 016 (`016_boot.asm`/`016_gdt.c`/`016_gdt.h`/`016_gdt_flush.asm`/`016_idt.h`/`016_idt.c`/`016_idt_flush.asm`/`016_isr0.asm`/`016_isr13.asm`/`016_isr14.asm`/`016_irq0.asm`/`016_irq1.asm`/`016_keyboard.h`/`016_keyboard.c`/`016_kheap.h`/`016_kheap.c`/`016_linker.ld`/`016_multiboot.h`/`016_multiboot.c`/`016_paging.h`/`016_paging.c`/`016_pic.h`/`016_pic.c`/`016_pit.h`/`016_pit.c`/`016_pmm.h`/`016_pmm.c`/`016_printf.h`/`016_printf.c`/`016_semaphore.h`/`016_semaphore.c`/`016_serial.h`/`016_serial.c`/`016_spinlock.h`/`016_spinlock.c`/`016_switch.asm`/`016_tss.h`/`016_tss.c`/`016_usermode.asm`/`016_vga.h`/`016_vga.c`, unchanged from Chapter 15; `016_task.h`/`016_task.c`/`016_syscall.h`/`016_isr_handlers.h`/`016_isr_handlers.c`/`016_kmain.c` -- changed; 0 new files this chapter, a first since Chapter 12). This chapter's own real free-frame count is 16084, unchanged from Chapter 15. 48 real source files total (6 changed, 42 carried forward unchanged from Chapter 15), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 17 (DONE, commit b6e8f80): "Per-Process Address Spaces: One Page Directory Per Task" -- stays in Part 7, closing a real gap Chapter 16 left open: every ring-3 task it ever created, including its own Ring3 Task A/B, still shared this kernel's ONE page directory, so nothing about the hardware itself kept one ring-3 task from reaching another's memory, only the fact that neither one tried. Grounded in the same OSDev Wiki citation Chapter 16 already used for TSS.ESP0 ("Kernel Multitasking," that page's own 32-bit context-switch example: "mov cr3,eax ; yes, load the next task's virtual address space" / "cmp eax,ecx ... je .doneVAS ; no ... so don't reload it and cause TLB flushes"), now applied to CR3 itself. New `paging_new_address_space()` (`017_paging.h`/`017_paging.c`) allocates a fresh page directory and copies all 1024 of the kernel's own real page-directory entries into it -- not just the 16 `paging_init()` itself builds, but whatever the kernel's own directory holds at the exact moment it is called, kheap growth and Chapter 8's own `0xC0000000` demo mapping included -- so every process still shares the kernel's own code/heap/drivers automatically, with room for private mappings no other process's directory will ever contain; `paging_map_page_in()` generalizes Chapter 8's own `paging_map_page()` to build mappings in any directory, not only whichever one CR3 currently holds, needed to construct a brand-new process's own private mappings before that process's directory is ever made active at all; `paging_translate_in()` is a new read-only directory walk letting `017_kmain.c` PROVE, from ring 0, exactly what physical frame a virtual address resolves to inside one specific process's own directory. New `task_create_process()` (`017_task.h`/`017_task.c`) replaces Chapter 16's `task_create_usermode()`: builds a process's own dedicated kernel stack FIRST (deliberately, since that `kmalloc()` call can itself grow the kheap, and `paging_new_address_space()` needs to copy the kernel's own directory AFTER any such growth), then its own new address space, then physically COPIES a whole, page-aligned, self-contained template function's compiled bytes into a fresh physical frame and maps that COPY at a fixed virtual address every process uses for its own code (`PROCESS_CODE_VADDR = 0xE8000000`, `017_task.h`) -- the same fixed address for every process, backed by a different physical frame each time. Surfaces and resolves, through explicit design-time reasoning, a genuinely subtle correctness question this book has not faced before: running compiled x86 code copied to a different virtual address than it was compiled for is only safe because near jumps/calls are PC-relative (internal control flow stays correct) and only the CODE moved, never kernel-resident data (a `.rodata` string reference stays correct, since every process's directory shares that mapping) -- but a CALL to a function NOT copied alongside would carry the WRONG PC-relative displacement, so `017_kmain.c`'s own two per-process templates (`process_template_normal()`, `process_template_reckless()`, each in its own new dedicated linker section in `017_linker.ld`, replacing Chapter 16's shared `.usermode_text`) inline their own `int $0x80` syscalls directly rather than calling a shared helper the way Chapter 16's ring-3 tasks did. Demonstrated with three real processes: Process A and Process B, running the IDENTICAL template at the IDENTICAL fixed virtual address, each backed by a different real physical frame -- proven directly with `paging_translate_in()` before either one runs a single instruction (the real captured run: `0xe8000000` resolves to physical `0x131000` in Process A's own directory, `0x135000` in Process B's) -- interleaving in perfect A/B alternation exactly like Chapter 16's own ring-3 tasks, with the same kind of independently-checkable lower bound on switch count (18 real switches against an expected minimum of 12, the excess honestly reported as real IRQ0 tick preemption); and Process Reckless, which runs one normal cycle and then deliberately reads a virtual address mapped in NO process's own directory, capturing this book's own newest flavor of real `#PF` -- error code `0x4`, non-present page, read, user mode, distinct from Chapter 10's supervisor/write/non-present (`0x2`) and Chapter 15's user/write/protection-violation (`0x5`) -- and halting the entire machine for good, the same honest ending Chapter 15's own one-off demo had. File 017 (`017_boot.asm`/`017_gdt.h`/`017_gdt.c`/`017_gdt_flush.asm`/`017_idt.h`/`017_idt.c`/`017_idt_flush.asm`/`017_isr0.asm`/`017_isr13.asm`/`017_isr14.asm`/`017_isr128.asm`/`017_irq0.asm`/`017_irq1.asm`/`017_isr_handlers.h`/`017_isr_handlers.c`/`017_keyboard.h`/`017_keyboard.c`/`017_kheap.h`/`017_kheap.c`/`017_multiboot.h`/`017_multiboot.c`/`017_pic.h`/`017_pic.c`/`017_pit.h`/`017_pit.c`/`017_pmm.h`/`017_pmm.c`/`017_printf.h`/`017_printf.c`/`017_semaphore.h`/`017_semaphore.c`/`017_serial.h`/`017_serial.c`/`017_spinlock.h`/`017_spinlock.c`/`017_switch.asm`/`017_syscall.h`/`017_tss.h`/`017_tss.c`/`017_usermode.asm`/`017_vga.h`/`017_vga.c`, unchanged in substance from Chapter 16 (comment-only touch-ups on `017_syscall.h`); `017_paging.h`/`017_paging.c`/`017_task.h`/`017_task.c`/`017_linker.ld`/`017_kmain.c` -- changed; 0 new files this chapter. This chapter's own real free-frame count is 16082 (two frames lower than Chapter 16's 16084, an honestly-reported consequence of this chapter's own larger kernel image). 48 real source files total (6 changed, 42 carried forward unchanged from Chapter 16), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 18 (DONE, commit 2dd94a3): "Loading a Real ELF Binary: Parsing Program Headers and Mapping PT_LOAD Segments" -- stays in Part 7, closing the last real gap Chapter 17 left open: every process it ever created still ran code THIS KERNEL IMAGE itself carried, baked into a dedicated linker section and physically copied out frame by frame -- nothing in that chapter ever loaded a genuinely separate, independently compiled program. This chapter does. `018_user_program.c` is compiled and linked completely apart from this kernel -- its own `gcc`/`ld` invocations, its own dedicated linker script fixing its own load address at `0xE9000000` -- and embedded whole into the GRUB ISO as a real Multiboot2 boot MODULE, cited field-for-field from the GNU Multiboot2 Specification's own "Modules" section ("This tag indicates to the kernel what boot module was loaded along with the kernel image, and where it can be found"). New `multiboot_find_module()` (`018_multiboot.h`/`018_multiboot.c`) walks the same fixed-header-then-tag-stream this book's own `multiboot_find_mmap()` has used since Chapter 7, watching for type 3 instead of type 6, and hands back the module's real physical `[mod_start, mod_end)` range GRUB already loaded it into. New `elf_load()` (`018_elf.h`/`018_elf.c`) is this chapter's own real payoff: reads that module's raw bytes as a genuine ELF32 header (OSDev Wiki, "ELF"), validates magic bytes/class/type/machine before trusting anything else in it, walks its real program headers, and maps every `PT_LOAD` segment into a process's own private directory at whatever virtual address THAT FILE's own `p_vaddr` specifies -- never a constant this kernel's own source chose; new `task_create_elf_process()` (`018_task.h`/`018_task.c`) uses it to give a process its own real entry point, read straight out of the file's own `e_entry` field, replacing Chapter 17's fixed `PROCESS_CODE_VADDR` convention for code (the fixed `PROCESS_STACK_VADDR` convention for the stack is unchanged). This chapter's own real testing surfaced and fixed a genuine bug, not a hypothetical one: a 100%-reproducible `#PF` (`CR2 = 0xe9000000`, error `0x7`) traced through a `gdb` postmortem to a GRUB module's own physical memory never being reserved in `018_pmm.c`'s allocator bitmap before `pmm_init()` handed out its first frame -- `paging_init()`'s own very next page-directory allocation landed squarely inside the module's own byte range and silently overwrote part of it before `elf_load()` ever read it. New `pmm_reserve_range()` (`018_pmm.h`/`018_pmm.c`), called in `018_kmain.c` immediately after `pmm_init()` and before this allocator ever hands out a single other frame, closes that gap -- verified over dozens of consecutive clean boots against a failure that was, before the fix, reproducible every single time. Demonstrated with two real processes, Process A and Process B, BOTH loading the SAME real ELF module, proven directly with `paging_translate_in()` before either one runs a single instruction that the file's own real `e_entry` (`0xe9000000`) resolves to physical `0x130000` in Process A's own directory and physical `0x135000` in Process B's -- the same claim Chapter 17 proved on a kernel-chosen constant, proved here on a genuinely loaded file's own address instead -- interleaving in perfect alternation with the same kind of independently-checkable lower bound on switch count (18 real switches against an expected minimum of 12, built from `018_user_program.h`'s own shared `USER_PROGRAM_ITERATIONS` constant so the two separately-compiled builds stay honest with each other). File 018 (40 files, unchanged from Chapter 17: `018_boot.asm`/`018_gdt.h`/`018_gdt.c`/`018_gdt_flush.asm`/`018_idt.h`/`018_idt.c`/`018_idt_flush.asm`/`018_irq0.asm`/`018_irq1.asm`/`018_isr0.asm`/`018_isr128.asm`/`018_isr13.asm`/`018_isr14.asm`/`018_isr_handlers.h`/`018_isr_handlers.c`/`018_keyboard.h`/`018_keyboard.c`/`018_kheap.h`/`018_kheap.c`/`018_paging.h`/`018_paging.c`/`018_pic.h`/`018_pic.c`/`018_pit.h`/`018_pit.c`/`018_printf.h`/`018_printf.c`/`018_semaphore.h`/`018_semaphore.c`/`018_serial.h`/`018_serial.c`/`018_spinlock.h`/`018_spinlock.c`/`018_switch.asm`/`018_syscall.h`/`018_tss.h`/`018_tss.c`/`018_usermode.asm`/`018_vga.h`/`018_vga.c`; `018_multiboot.h`/`018_multiboot.c`/`018_task.h`/`018_task.c`/`018_pmm.h`/`018_pmm.c`/`018_linker.ld`/`018_kmain.c` -- changed (8 files); `018_elf.h`/`018_elf.c`/`018_user_program.h`/`018_user_program.c`/`018_user_program.ld` -- new (5 files)). This chapter's own real module reservation is visible directly in its own real allocator output: the module occupies physical `0x10d000`-`0x10e304` (two frames), and the next three ordinary frames this allocator hands out (`0x10b000`, `0x10c000`, `0x10f000`) correctly skip over both. 53 real source files total (5 new, 8 changed, 40 carried forward unchanged from Chapter 17), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; 0 diagram-checker violations; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 19 (DONE, commit d0e37ad): "A Real Disk Driver: ATA PIO Mode, Reading and Writing Real Sectors" -- opens a new arc toward persistent storage: every byte this kernel had ever touched across eighteen chapters lived in RAM alone, gone the instant QEMU exited. This chapter's scope was confirmed with the user in two rounds (general topic: filesystem/disk driver; split: disk driver only this chapter, a real filesystem deferred to Chapter 20). New `019_ata.h`/`019_ata.c` is a real PIO-mode ATA/IDE driver for the primary bus's master drive, cited field-for-field from OSDev Wiki's own "ATA PIO Mode" page: the real port map (`0x1F0`-`0x1F7`, alternate status `0x3F6`), status bits (`BSY`/`DRQ`/`DF`/`ERR`), command bytes (`IDENTIFY` `0xEC`, `READ SECTORS`/`WRITE SECTORS` `0x20`/`0x30`, `Cache Flush` `0xE7`), the 400ns "read the alternate status port fifteen times" drive-select delay, and the write path's own required per-word delay plus mandatory post-write Cache Flush so data reaches the backing disk image rather than sitting only in the emulated drive's write cache. Deliberately stays polled, not interrupt-driven -- IRQ14 is never wired up, an explicit stated scope limit (this book's established pattern, e.g. Chapter 5's PIC deviation, Chapter 13's spinlock) since `ata_read_sector()`/`ata_write_sector()` already block until their real transfer completes, with no capability this chapter's own demo needs from an interrupt. Before writing the driver, this chapter empirically confirmed -- via a real boot test with both `-cdrom os.iso` and a new `-drive file=disk.img,format=raw,if=ide,index=0 -boot d` attached -- that the existing GRUB boot ISO lives on the secondary bus and the new virtual disk lands on the primary bus this driver was written for, rather than assuming it. `019_kmain.c`'s own new demo, appended after the existing (Chapter 18, carried forward unmodified) ELF/process demo: `ata_identify()` confirms a real drive, a real non-repeating 512-byte pattern is written to LBA 100 via `ata_write_sector()`, and read back via `ata_read_sector()` into a SEPARATE, zero-initialized buffer this kernel never wrote to -- all 512 bytes matched in the real captured run. Verified a second, independent way beyond the kernel's own self-report: `build/disk.img`'s raw bytes were read directly in Python, completely outside QEMU, at the real byte offset LBA 100 maps to, and matched the expected pattern exactly -- genuine proof the write reached persistent storage, not just RAM. A real, same-boot matched serial-log/VGA-screenshot pair (captured via QEMU's own monitor `screendump`, same lesson from Chapter 18's own capture-timing bug applied again) confirms the identical text on the emulated VGA console. File 019 (52 files, unchanged from Chapter 18: every `019_*` file except the three below); `019_kmain.c` -- changed (1 file: new `#include "019_ata.h"`, rewritten top-of-file chapter comment, and the new disk-demo section appended at the end of `kmain()`); `019_ata.h`/`019_ata.c` -- new (2 files). 55 real source files total (2 new, 1 changed, 52 carried forward unchanged from Chapter 18), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 20 (DONE, commit a7f4fb1): "A Real Filesystem: FAT16, Files Addressed by Name Instead of by Raw LBA" -- closes the gap Chapter 19 deliberately left open: that chapter's own real ATA driver could read or write any real sector, but nothing about it knew what a "file" was, or which sectors belonged to which one. This chapter's scope was confirmed with the user in two rounds (filesystem type: FAT16; scope: read + write, flat namespace, no subdirectories -- explicitly including create/write/read/list/delete by name in one root directory, subdirectories deferred to a later chapter). New `020_fat16.h`/`020_fat16.c` is a real, standards-compliant FAT16 filesystem, cited field-for-field from OSDev Wiki's own "FAT" page: the real boot sector/BPB field layout (offsets 0-510, boot signature `0xAA55` at offset 510), the real 32-byte directory entry layout, the real `root_dir_sectors`/`first_data_sector`/`first_sector_of_cluster` formulas, and the real FAT16 special values (free `0x0000`, end-of-chain `>= 0xFFF8` -- this driver always writes `0xFFFF` specifically --, bad `0xFFF7`, directory byte-0 `0x00` end-of-directory/`0xE5` deleted). Built entirely on top of Chapter 19's own `ata_read_sector()`/`ata_write_sector()` -- this file never touches an I/O port directly. This chapter's own disk is 8 MiB (16384 sectors), deliberately larger than Chapter 19's own 1 MiB, chosen specifically to cross the real 4085-usable-cluster threshold the standard convention uses to distinguish FAT16 from FAT12 -- Chapter 19's own smaller disk could never have supported a genuinely standards-compliant FAT16 volume at any cluster size. This chapter's own geometry (1 sector/cluster, 2 FATs * 64 sectors, 512-entry/32-sector root directory) yields exactly 16223 usable clusters, comfortably inside the real FAT16 range, with every sector accounted for and none wasted. `020_kmain.c`'s own new demo, appended after the existing (Chapter 19, carried forward unmodified) raw-sector ATA demo: formats a fresh volume, creates a one-cluster `HELLO.TXT` and a three-cluster `BIGFILE.BIN` (proving real cluster-CHAIN walking, not just a single sector), lists the root directory, reads both files back byte-for-byte into buffers this kernel never wrote to directly, deletes `HELLO.TXT` and confirms `fat16_read_file()` correctly refuses it afterward, then creates `REUSE.TXT` -- the same size as the deleted file -- and proves it deterministically reuses `HELLO.TXT`'s own exact freed cluster, the same "matches the freed frame?" proof this book has run since Chapter 7's own physical memory allocator. Verified a second, independent way beyond the kernel's own self-report, twice (once against an initial test disk image, once against the official captured one): `build/disk.img`'s raw bytes were read directly in Python, completely outside QEMU -- the real boot sector fields, both real mirrored FAT copies (confirmed byte-for-byte identical, with cluster 2's own real chain matching the kernel's own reported assignments), the real root directory's own 32-byte entries (including the deleted `HELLO.TXT` entry's own real `0xE5` marker, superseded but not erased), and the real cluster data itself (including `BIGFILE.BIN`'s own zero-padded final partial cluster) all matched what the kernel reported. A real, same-boot matched serial-log/VGA-screenshot pair (captured via QEMU's own monitor `screendump`, the same lesson from Chapter 18's own capture-timing bug applied again) confirms the identical text on the emulated VGA console. File 020 (52 files, unchanged from Chapter 19: every `020_*` file except the three below); `020_kmain.c` -- changed (1 file: new `#include "020_fat16.h"`, rewritten top-of-file chapter comment, and the new FAT16-demo section appended at the end of `kmain()`); `020_fat16.h`/`020_fat16.c` -- new (2 files). 55 real source files total (2 new, 1 changed, 52 carried forward unchanged from Chapter 19), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 21 (DONE, commit ef5a71b): "Real Subdirectories: One Level of Nesting Inside FAT16" -- lifts the flat-root-only limit Chapter 20 deliberately left in place: that chapter's own real FAT16 driver could already create, read, delete, and list files by name, but every one of them lived in exactly one place, the root directory. This chapter's scope was confirmed with the user in two rounds (topic: real subdirectories; split: minimal, tightly scoped -- exactly one level of nesting, no `fat16_rmdir()`, no multi-level paths). OSDev Wiki's own "FAT" page, cited field-for-field for every part of the base layout Chapter 20 already used, documents the `ATTR_DIRECTORY` (0x10) bit but never actually describes a real subdirectory's own contents, so this chapter's own new subdirectory conventions are cited instead from the primary source OSDev's own page itself links to: the Microsoft FAT specification, Section 6 ("FAT Directory Structure"), Section 6.2 (`ATTR_DIRECTORY`'s own real meaning), and Section 6.5 (the "." and ".." entry conventions, and a subdirectory's own cluster chain growing exactly like a file's). `021_fat16.c` generalizes Chapter 20's own root-only `scan_root()` into `scan_dir()`/`dir_sector_lba()`, the one place this file's own code has to know the real difference between the root's fixed, never-growing sectors and an ordinary subdirectory's own growable cluster chain -- and deliberately represents the root directory internally as the sentinel value `0` throughout, chosen because it is the EXACT real on-disk value the specification itself requires a ".." entry to hold when the parent is the root (Section 6.5: "the DIR_FstClusLO and DIR_FstClusHI contents must be set to 0"), not an arbitrary internal convention layered on top. New `fat16_mkdir()` allocates a real cluster and writes it a real "." entry (self-referencing its own new cluster) and ".." entry (cluster 0, the root), with `DIR_FileSize` written as 0 per Section 6.5 even though a genuine cluster is allocated; refuses outright -- never silently creates intermediate directories -- if `name` contains a `'/'`. New `resolve_path()` splits a `"DIR/NAME.EXT"` argument at its first `'/'` only, so `fat16_create_file()`/`fat16_read_file()`/`fat16_delete_file()` now resolve one real level of nesting; each refuses outright, rather than guessing, when a named directory component doesn't exist or isn't really a directory, and `fat16_read_file()`/`fat16_delete_file()` each also refuse outright when the resolved entry is itself a real directory -- there is no `fat16_rmdir()` this chapter, deleting a directory is explicit, stated out-of-scope. `021_kmain.c`'s own new demo, appended after the existing (Chapter 20, carried forward unmodified) flat-root file demo: creates a real subdirectory `DOCS`, creates `DOCS/NOTES.TXT` inside it and reads it back byte-for-byte, lists `DOCS` directly off disk (showing its own real "."/".." entries, "." reading back with cluster 6 -- self-referencing its own directory -- and ".." reading back with the real literal cluster 0), creates a second, distinct root-level `NOTES.TXT` with the same leaf name (proving two genuinely different real files in two different real directories), then exercises all three of this chapter's own stated refusal boundaries for real: a nested `fat16_mkdir("DOCS/SUB")`, a `fat16_delete_file("DOCS")` on a real directory, and a `fat16_read_file("NOPE/MISSING.TXT")` through a directory component that was never created -- each refused in exactly the words the real captured run shows. Verified a second, independent way beyond the kernel's own self-report, twice (once against an initial test disk image, once against the official captured one): `build/disk.img`'s raw bytes were read directly in Python, completely outside QEMU -- the real boot sector fields, both real mirrored FAT copies, every cluster in this chapter's own new chains (2 through 8) matching the kernel's own reported assignments, the real root directory's own entries (including `DOCS`'s own real `ATTR_DIRECTORY` bit, first cluster 6, and `DIR_FileSize` genuinely 0), `DOCS`'s own real cluster 6 read directly and its raw "."/".." entries confirmed byte-for-byte (not merely as this kernel's own printed claim), and the real cluster data for both `DOCS/NOTES.TXT` and the root's own `NOTES.TXT` matching what `kmain.c` wrote to each. A real, same-boot matched serial-log/VGA-screenshot pair (captured via QEMU's own monitor `screendump`, the same lesson from Chapter 18's own capture-timing bug applied again) confirms the identical text on the emulated VGA console. File 021 (54 files, unchanged from Chapter 20: every `021_*` file except the three below); `021_fat16.h`/`021_fat16.c`/`021_kmain.c` -- changed (3 files: new `ATTR_DIRECTORY` bit and subdirectory-aware helpers in `021_fat16.h`/`021_fat16.c`, rewritten top-of-file chapter comments, and the new subdirectory-demo section appended at the end of `kmain()`). 57 real source files total (0 new, 3 changed, 54 carried forward unchanged from Chapter 20), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 22 (DONE, commit 36b9afc): "Removing Real Subdirectories: fat16_rmdir() and the Empty-Directory Rule" -- lifts the other limit Chapter 21 deliberately left in place: that chapter's own real subdirectory support could create, populate, read, and list one level of nesting, but a real directory, once created, could never be removed -- `fat16_delete_file()` refused outright whenever asked to remove one. This chapter's scope was confirmed with the user in two rounds (topic: real `fat16_rmdir()`; split: minimal, tightly scoped -- deletion only, still exactly one level of nesting, no recursive multi-level path walker). Neither of this book's two established FAT citation sources actually documents directory removal: OSDev Wiki's own "FAT" page and the Microsoft FAT specification are both real on-disk FORMAT specifications, not filesystem-driver ALGORITHM guides -- cited directly, the specification's own Overview states it "does not describe all algorithms contained in the Microsoft FAT file system driver implementation". So this chapter's own "a directory must be empty before it can be removed" rule is cited instead from a different, genuinely appropriate real specification: IEEE Std 1003.1-2008 (POSIX.1-2008)'s own `rmdir()` specification, fetched directly from the Open Group's own online publication and quoted directly ("If the directory is not an empty directory, rmdir() shall fail and set errno to [EEXIST] or [ENOTEMPTY]"). `022_fat16.c`'s new `is_dir_empty()` treats a directory's own real "."/".." entries as expected content, not clutter -- any OTHER real occupied entry (not the `0x00` end marker, not the `0xE5` deleted marker) means "not empty". New `fat16_rmdir(name)` refuses outright -- never partially acts -- on a nested path (the same one-level boundary `fat16_mkdir()` already enforces), a non-directory target (directs the caller to `fat16_delete_file()` instead), a non-existent name, or (checked BEFORE any cluster is freed or any entry is touched, preserving this book's established honest no-partial-effect refusal discipline) a non-empty directory; on success, frees the directory's ENTIRE real cluster chain back to both FAT copies -- not merely its first cluster, reasoned through because a subdirectory can genuinely grow past one cluster the same way a file does -- and marks its own root entry with the real `0xE5` "deleted" marker. `fat16_mkdir()`'s own signature gained an `out_first_cluster` out-parameter (mirroring `fat16_create_file()`'s existing convention) purely so this chapter's own demo could prove cluster reuse in code, not merely in printed text. `022_kmain.c`'s own new demo, appended after Chapter 21's existing subdirectory demo: creates and removes a genuinely empty directory `EMPTYD`; exercises all four of this chapter's own stated refusal boundaries for real -- `fat16_rmdir("DOCS")` while `DOCS` still holds a real file, `fat16_rmdir("REUSE.TXT")` on an ordinary file, `fat16_rmdir("NOPE")` on a name that never existed, and a nested `fat16_rmdir("DOCS/SUB")` -- each refused in exactly the words the real captured run shows; empties `DOCS` for real via `fat16_delete_file("DOCS/NOTES.TXT")`, removes `DOCS` for real, then creates a new directory `REDOCS` and captures its own real first cluster, comparing it directly (in code, not merely printed) against `DOCS`'s own former first cluster -- the same "matches the freed X?" proof this book has run on every real allocator since Chapter 7's own physical memory manager, extended this chapter to prove reuse of a whole directory's cluster, not merely a file's. Verified a second, independent way beyond the kernel's own self-report: `build/disk.img`'s raw bytes were read directly in Python, completely outside QEMU -- every FAT entry's final state (including clusters 7 and 9, `DOCS/NOTES.TXT`'s and `EMPTYD`'s own former clusters, both genuinely FREE again), the real root directory's own entries (confirming `DOCS` and `EMPTYD` both genuinely absent as live entries, `REDOCS` present with the real `ATTR_DIRECTORY` bit set, first cluster matching `DOCS`'s own former cluster exactly, and `DIR_FileSize` genuinely 0), `EMPTYD`'s own former root slot still carrying the real, literal `0xE5` marker untouched since its own removal, and `REDOCS`'s own raw "."/".." entries in its reused cluster. This verification pass also caught and fixed a bug in its OWN first assumption (not a kernel bug): `scan_dir()`'s own free-slot search (unchanged since Chapter 21) returns the FIRST reusable root slot scanning forward, so `REDOCS`'s own new root entry reuses not just `DOCS`'s freed CLUSTER but `DOCS`'s exact freed ROOT DIRECTORY SLOT too (index 2) -- confirmed directly from the raw bytes and folded into the chapter's own narrative as a genuine bonus fact, not merely assumed from the matching cluster number alone. A real, same-boot matched serial-log/VGA-screenshot pair (captured via QEMU's own monitor `screendump`) confirms the identical text on the emulated VGA console. File 022 (54 files, unchanged from Chapter 21: every `022_*` file except the three below); `022_fat16.h`/`022_fat16.c`/`022_kmain.c` -- changed (3 files: new `fat16_rmdir()`/`is_dir_empty()` in `022_fat16.h`/`022_fat16.c`, `fat16_mkdir()`'s new out-parameter, rewritten top-of-file chapter comments citing POSIX, and the new rmdir demo appended at the end of `kmain()`). 57 real source files total (0 new, 3 changed, 54 carried forward unchanged from Chapter 21), cloud-sandbox-only per the established toolchain-availability finding; `mkdocs build --strict` clean; all code/output blocks verified byte-for-byte against their locked sources.

Chapter 23 (DONE, commit 21a3c6f): "Recursive Multi-Level Paths: Lifting FAT16's One-Level Scope" -- lifts the one real limit Chapters 21 and 22 both shared: `fat16_mkdir()`/`fat16_rmdir()` each refused outright the instant a name held more than one real '/', and `fat16_create_file()`/`fat16_read_file()`/`fat16_delete_file()` could resolve exactly one real level of nesting and no deeper. Scope confirmed via two AskUserQuestion rounds: recursive multi-level paths; minimal/tightly scoped. Neither OSDev Wiki's "FAT" page nor the Microsoft FAT specification says anything about parsing a multi-component path at all -- both describe the real on-disk FORMAT, not how a driver should interpret the string an application hands it (confirmed directly against OSDev's own page, which never mentions "/"-separated paths). So this chapter's own new, arbitrary-depth `resolve_path()` is cited instead from IEEE Std 1003.1-2008 (POSIX.1-2008), Base Definitions, Section 4.11, "Pathname Resolution", fetched directly from the Open Group's own online publication and quoted directly: "Each filename in the pathname is located in the directory specified by its predecessor ... Pathname resolution shall fail if this cannot be accomplished." Because `fat16_create_file()`/`fat16_read_file()`/`fat16_delete_file()` already called `resolve_path()` since Chapter 21, generalizing that ONE function was enough to lift their own one-level limit with zero changes inside any of the three; `fat16_mkdir()`, `fat16_rmdir()`, and `fat16_list_dir()` were each updated to call it instead of always assuming the root. This surfaced a real correctness fix: a subdirectory's own ".." entry can no longer be hardcoded to the root's own sentinel cluster 0, since a directory can now genuinely have a parent that is itself not the root -- `fat16_mkdir()` now writes the real, resolved parent cluster instead, cited directly (Microsoft FAT specification, Section 6.5, the general "must be the same as that of the parent" rule, not merely its root-parent special case). `023_kmain.c`'s own new demo creates three real, genuinely nested subdirectories (LEVEL1, LEVEL1/LEVEL2, LEVEL1/LEVEL2/LEVEL3) one `fat16_mkdir()` call at a time, creates and reads back a real file three real levels deep, lists at that depth, exercises this chapter's own two new stated refusal boundaries (an intermediate component that was never created; one that names a real file, not a directory), demonstrates a nested mkdir (`REDOCS/SUB`) that Chapter 21 would have refused outright now succeeding for real, and removes the whole nested chain again bottom-up with real `fat16_rmdir()` calls. Two lines from Chapters 21's and 22's own carried-forward demos that used to prove a nested-path REFUSAL were deliberately removed (each with an explanatory comment), since this chapter's own change makes those exact calls succeed instead -- leaving a stale, now-false refusal claim printed would have contradicted this chapter's own real captured output. No kernel bugs -- worked first boot. Independent Python verification against disk.img's raw bytes confirmed every new/freed cluster (7, 9, 10, 11 freed; 12 still allocated) and, critically, that REDOCS/SUB's own real ".." entry reads back as cluster 6 (REDOCS itself) rather than the old hardcoded 0 -- proving the parent-cluster fix is correct on real, on-disk bytes, not merely unexercised. 57 real source files, 0 new: 3 changed (fat16.h/.c, kmain.c), 54 carried forward from Ch22. Device sync: 61 files across two batches, all verified.

Chapter 24 (DONE, commit a9fdbfc): "PCI Bus Enumeration: Finding the Hardware Before You Can Drive It" -- the first step of the user's own stated future capstone networking chapter, scope confirmed via two AskUserQuestion rounds: capstone networking chapter (topic); minimal/tightly scoped (split). Every driver this book has written before this chapter -- Chapter 19's own ATA PIO disk driver above all -- talks to a fixed, hardcoded I/O port range known in advance by PC platform convention; a real network card has no such fixed address at all, only a fixed MECHANISM for asking where everything else lives. New `024_pci.h`/`024_pci.c`, cited field-for-field from OSDev Wiki's own "PCI" page (https://wiki.osdev.org/PCI): `pci_config_read_dword()` builds a real `CONFIG_ADDRESS` value (port 0xCF8) field-for-field per its cited bit layout (bit 31 enable, bits 23-16 bus, bits 15-11 device, bits 10-8 function, bits 7-0 register offset with its own low two bits forced to 0) and reads back a real dword from `CONFIG_DATA` (port 0xCFC) -- this kernel's first-ever 32-bit port I/O (every earlier driver only ever used 8-bit/16-bit port I/O), since `CONFIG_DATA` always hands back a full 32-bit dword regardless of how narrow the field wanted is, decoded by shifting/masking in software. `pci_probe_function()` decodes vendor ID, device ID, class code, subclass, and header type from three real dword reads (offsets 0x00/0x08/0x0C), checking the real Vendor ID = 0xFFFF sentinel cited directly ("Since there are no vendors that == 0xFFFF, it must be a non-existent device"). `pci_enumerate()` performs a real brute-force scan of all 256 buses x 32 devices (8192 real coordinates), cited directly from the page's own brute-force pseudocode, checking every real function 1-7 of a device too when its own Header Type byte sets the real Multi-Function bit. `pci_find_by_class()` runs the same scan, stopping at the first real device function whose class code/subclass match -- the real entry point a future driver chapter would call first. `024_kmain.c`'s own new demo runs after every earlier chapter's own carried-forward FAT16 demo, unchanged: calls `pci_enumerate()` (finds exactly 6 real device functions on this machine), then `pci_find_by_class()` to locate the real IDE controller Chapter 19's own driver has always talked to via fixed ports (found at 0:1.1, independently of those fixed ports) and a real Ethernet controller -- new to this chapter's own QEMU command line (`-netdev user,id=n0 -device rtl8139,netdev=n0`), found at 0:3.0, vendor 10ec device 8139, the real starting point for a future network driver chapter -- and a real refusal-boundary demo, a USB controller (class 0x0C, subclass 0x03) genuinely absent from this machine, correctly not found. No kernel bugs -- worked on first boot attempt. This chapter's own verification could not follow the established raw-disk-byte pattern (disk.img is byte-identical to Chapter 23's own, since the FAT16 demo code is unchanged) because PCI bus state is live hardware state, never written to disk -- instead verified independently against QEMU's own monitor `info pci` command, queried directly from the same running instance as the serial capture, confirming all 6 real device functions at the exact same bus:device.function coordinates and vendor:device IDs this kernel's own enumeration reported, including real BAR values (IDE controller I/O base 0xc100, RTL8139 I/O base 0xc000) this chapter's own driver does not yet read. 58 real source files: 2 new (pci.h/pci.c), 1 changed (kmain.c), 55 carried forward unchanged from Chapter 23.

Chapter 25 (DONE, commit 8b92437): "A Real RTL8139 Driver: One Real Frame, Sent and Received" -- the user's own stated capstone networking chapter's own second and final confirmed step, scope confirmed via two AskUserQuestion rounds: a real RTL8139 driver, send/receive one real frame (topic); minimal/tightly scoped, even just looped back (split). New `025_rtl8139.h`/`025_rtl8139.c`, cited field-for-field from OSDev Wiki's own "RTL8139" page and, wherever that page is silent (the Transmit Configuration Register above all, which it never mentions at all), the real Realtek RTL8139D(L) datasheet Rev. 1.11 it is itself derived from. Two real problems solved that no earlier chapter's own driver ever faced: (1) a real DMA buffer needs a real physical address, and this kernel's own `kmalloc()` heap (non-identity-mapped, starting at 0xD0000000) cannot supply one -- this driver uses `pmm_alloc_frame()` directly instead, with an added real contiguity check across three consecutive frame allocations for its own 12 KiB receive ring; (2) proving a real send and receive with no real network on the other end of the wire, solved with the device's own real hardware loopback mode (TCR bits 18-17, "11: Loopback mode", cited directly from the real datasheet). `025_pci.h`/`025_pci.c` gained one new function, `pci_config_write_dword()` -- the write half of Configuration Mechanism #1 this kernel had never needed before Chapter 24's own read-only driver, used here to enable the real PCI Command register's I/O Space and Bus Master bits. `025_kmain.c`'s own new demo runs after Chapter 24's own carried-forward PCI enumeration: brings the real device up, sends one real 60-byte Ethernet frame (EtherType 0x88B5, cited directly from RFC 5342 Appendix B.2), and reads back 64 real bytes (60 real payload bytes plus a real 4-byte hardware-appended CRC) via the device's own real loopback path -- every one of the original 60 bytes matches. One real, honest gap found by testing, not by citation: neither of this chapter's own two real sources documents any ordering requirement between writing TCR's own loopback bits and enabling CMD's RE/TE bits, and this exact QEMU environment genuinely silently drops the loopback bits if TCR is written first (readback: bits present with CMD-first ordering, absent with TCR-first) -- found via a bounded diagnostic poll (not part of the final driver), fixed by writing TCR strictly after CMD, and reported in the chapter text exactly as found rather than presented as a datasheet fact. This chapter's own verification could not use a single built-in QEMU monitor command the way Chapter 24 used `info pci` (no command reports "was this exact frame sent and received") -- instead verified independently via QEMU's own monitor `xp` (examine physical memory) command, reading the exact real physical receive-ring address (0x142000) directly, confirming the real received bytes (header, both real MAC addresses, EtherType, ASCII payload, and the real trailing CRC) entirely outside this kernel's own code; a second `info network` monitor capture independently confirmed the real device's own MAC address. No kernel bugs in the final driver logic itself -- the one real issue found (TCR write ordering) was diagnosed and fixed before any committed code was written. 61 real source files: 2 new (rtl8139.h/rtl8139.c), 2 changed (pci.h/pci.c gained one function, kmain.c gained one demo), 57 carried forward unchanged from Chapter 24.

Chapter 26 (DONE, commit e49736f): "Interrupt-Driven RTL8139: A Real hlt, Woken by Real Hardware" -- scope confirmed via two AskUserQuestion rounds: interrupt-driven RTL8139 (topic); minimal/tightly scoped (split). Upgrades Chapter 25's own real, working polled RTL8139 driver to this book's first genuinely interrupt-driven device wait, replacing both of Chapter 25's own tight register spins (`TSD0`, `ISR`) with a real `hlt`, woken only by a real hardware interrupt. Three new real pieces this book had never assembled before: (1) a real PCI Interrupt Line lookup (configuration-space offset 0x3C), cited directly from OSDev Wiki's own "PCI" page, verified at runtime against a fixed compile-time vector rather than dynamically installed -- a real, honestly-stated architectural gap, since this kernel's own `026_idt.c` only ever installs IDT gates once, at compile time, with no mechanism yet for a driver to register a new gate once it discovers its own real IRQ number; (2) this book's first-ever gate for a SLAVE-PIC line (IRQ 8-15) -- new `026_irq11.asm` stub, new vector 0x2B gate in `026_idt.c` (IRQ 11 is the 4th slave line, 11-8=3, landing at 0x28+3 given this kernel's own `pic_remap(0x20, 0x28)`) -- and the real cascade-unmask requirement a slave line needs that no earlier master-only driver (IRQ0/IRQ1) ever needed, cited directly from OSDev Wiki's own "8259 PIC" page ("Masking IRQ2 will cause the Slave PIC to stop raising IRQs"); (3) a real interrupt handler, `026_rtl8139.c`'s own new `irq11_handler()`, acknowledging real ISR bits before anything else touches the device again, cited directly from the same OSDev "RTL8139" page Chapter 25 already used. The expected IRQ number (11) was not guessed -- independently corroborated ahead of time from Chapter 24's own already-captured real QEMU monitor `info pci` output ("IRQ 11, pin A"), then re-confirmed live by this chapter's own new runtime PCI read. A real, genuine race condition was found and fixed during design, before any test ever ran: QEMU's own emulated RTL8139 loopback mode delivers a received frame SYNCHRONOUSLY, as a side effect of the transmit-descriptor write itself (established during Chapter 25's own prior debugging), so `rok_pending` could already be set before `rtl8139_receive_first_packet()` is ever called -- fixed by moving that flag's own reset into `rtl8139_send()` (paired with `tok_pending`'s own reset) rather than resetting it at the top of the receive-wait function, which would have silently discarded an already-arrived real event and hung forever. New `026_rtl8139.get_irq_count()` gives the chapter's own demo a real, checkable number ("count it and check it", this book's own style since Chapter 11/12's own context-switch counts) -- exactly 1 real IRQ 11 delivery, carrying both TOK and ROK together, predicted in the code's own comment before the real captured run confirmed it exactly. No kernel bugs -- clean build and successful boot on the first attempt. This chapter's own independent verification reached one level outside the kernel's own self-report: QEMU's own monitor `info pic` command, read directly from the real emulated 8259 hardware state (not this kernel's own report of it), confirming `pic0: imr=f8` (IRQ0/1/2 unmasked, no others) and `pic1: imr=f7` (only IRQ11, bit 3, unmasked) -- the real cascade line and this device's own real line, and no others, genuinely unmasked at the hardware level. 61 real source files: 1 new (`026_irq11.asm`), 4 changed (`026_idt.h`/`026_idt.c` gained one gate, `026_rtl8139.h`/`026_rtl8139.c` upgraded from polled to interrupt-driven, `026_kmain.c` gained one new reported number), 56 carried forward unchanged from Chapter 25.

Chapter 27 (DONE, commit 9b96be3): "Multi-Frame RTL8139: Real Round-Robin Transmit, Real CAPR Wraparound" -- scope confirmed via two AskUserQuestion rounds: multi-frame RTL8139 (topic); minimal/tightly scoped (split). Lifts two of the three real scope limits Chapter 25 stated and Chapter 26 left untouched: no more than one frame in flight at a time, and no real CAPR advancement or receive-ring wraparound at all (Chapters 25/26's own driver only ever read the very first packet a freshly reset ring received). `027_rtl8139.h`/`027_rtl8139.c` add real per-descriptor round-robin transmit (`rtl8139_send_queue()`, cycling TSAD0-3/TSD0-3 at I/O offsets 0x20/0x24/0x28/0x2C and 0x10/0x14/0x18/0x1C) and real CAPR-based receive-ring advancement (`rtl8139_receive_next_packet()`, `RTL8139_RX_RING_NOMINAL_SIZE = 8192u`), cited field-for-field from OSDev Wiki's own "RTL8139" page for the round-robin descriptor mechanism and, for the real CAPR "-16" convention OSDev never states a reason for, from a real October 1999 Realtek Linux driver mailing-list exchange between Daniel Kobras and Donald Becker (beowulf.org archive), independently corroborated by two further real open-source driver sources and by a real 2013 QEMU development mailing-list patch thread (`s->RxBufPtr = MOD2(val + 0x10, s->RxBufferSize)`). Two genuine, reproducible bugs were found only through live instrumentation of this exact QEMU 8.2.2 environment's own running hardware state, neither discoverable from any documentation: (1) a correctly-delivered real received packet's own header STATUS half (bit 0 = ROK) reads back `0x0000` in this environment even on a genuine, correctly-length packet -- confirmed via QEMU monitor `xp` memory inspection -- worked around by keying the receive wait on the header's own LENGTH half instead (safe because the ring is fully zeroed at init and after each packet is consumed, and no real frame this driver sends can ever produce a genuine length of 0); (2) retriggering the SAME real transmit descriptor a second time in a row, with no other real descriptor's transmission in between, leaves that second transmission genuinely stuck forever (OWN cleared, TOK never set, ISR 0x0) -- found via direct `kprintf` instrumentation of `rtl8139_send()`/`rtl8139_wait_descriptor_sent()`, worked around by having `027_kmain.c`'s own demo round-robin every fresh send across all 4 real descriptors via `rtl8139_send_queue()` instead of reusing `rtl8139_send()` (left otherwise unchanged/correct for genuine single-shot use, with its own code comment now documenting the limitation honestly) back-to-back. The `rok_pending`/`tok_pending` software flags Chapter 26 introduced were removed entirely -- `rtl8139_send_queue()`'s own batched sends can coalesce more than one real completion event into fewer real interrupt deliveries than there are events, which a boolean flag cannot represent, so every wait now checks real, persistent hardware/ring state directly instead. `027_kmain.c`'s own new two-part demo: Part 1 queues 4 real frames back-to-back with no waiting between them (genuinely more than one frame in flight at once), predicts a real IRQ11-delivery RANGE of 1 to 4 rather than a fixed number (since this environment may coalesce completion events), and observed exactly 4; Part 2 sends and receives 140 real frames total (4 leftover from Part 1 plus 136 fresh round-robined round trips), deliberately more than the 8192-byte nominal ring size, with 0 byte-for-byte mismatches and the real receive-ring read position crossing the ring boundary exactly once (0x0 to 0x2530, 9520 real bytes advanced), for 140 total real IRQ11 deliveries across the whole chapter. A real QEMU tooling bug was also found and fixed along the way: the monitor's own `screendump` command silently writes a PPM file regardless of a `.png` filename unless `-f png` is passed explicitly, discovered via a throwaway instance's own `help screendump` output. This chapter's own independent verification reused Chapter 26's own established pattern: QEMU's own monitor `info pic` command, read directly from the real emulated 8259 hardware state, confirmed `pic0: imr=f8`/`pic1: imr=f7` unchanged from Chapter 26 -- proving this chapter's own changes disturbed no interrupt-mask state. A real, same-boot matched serial-log/VGA-screenshot pair (captured via QEMU's own monitor `screendump -f png`) confirms the identical text on the emulated VGA console. 62 real source files: 0 new, 3 changed (`027_rtl8139.h`/`027_rtl8139.c` gained round-robin transmit and ring-wraparound receive plus both empirical fixes, `027_kmain.c` gained the two-part demo), 59 carried forward unchanged from Chapter 26.

Chapter 28 (DONE, commit 2f0f43c): "Address Resolution Protocol: A Real ARP Request/Reply Over a Real Wire" -- scope confirmed via two AskUserQuestion rounds: the offered dynamic IDT-gate-installation topic was declined ("Something else"); the user redirected to networking (topic: "Networking further"); scope narrowed to ARP only (split: "minimal, tightly scoped: a real ARP request/reply exchange... the smallest genuine next layer above raw Ethernet frames"). New `028_arp.h`/`028_arp.c`, the first real protocol layer above the raw Ethernet frames every RTL8139 chapter before it has sent and received, cited field-for-field from two primary sources: RFC 826 ("An Ethernet Address Resolution Protocol", 1982) for the real packet field names/order (ar$hrd/ar$pro/ar$hln/ar$pln/ar$op/ar$sha/ar$spa/ar$tha/ar$tpa), real opcode values (REQUEST=1/REPLY=2), and the real broadcast requirement ("broadcast to all stations on the Ethernet cable"); and OSDev Wiki's own "Address Resolution Protocol" page for the same fields as a real compilable struct and the real practical detail that a request's own ARP-payload target hardware address is zeroed (not the Ethernet frame's own destination, which is broadcast instead). One real citation gap neither source fills -- the frame-level EtherType -- was closed from a third, authoritative primary source per this book's established pattern: IANA's own official IEEE 802 Numbers registry, EtherType 0x0806 for ARP (assignment authority RFC 9542). The real IP address resolved, QEMU's own default gateway 10.0.2.2, is cited directly from QEMU's own official documentation rather than assumed. Real ARP fields are all built/parsed by explicit big-endian byte writes/shifts (never a packed struct laid over wire bytes), matching this kernel's own little-endian x86 registers and the same style `028_kmain.c`'s own EtherType handling and `028_pci.c`'s own CONFIG_DATA reads have used since Chapters 24/27. Getting a real reply from a real host genuinely outside this device required a real architecture change: `rtl8139_init()` gained a new `enable_loopback` parameter (both real TCR values -- `TCR_LOOPBACK_ON`/`TCR_NORMAL_OPERATION` -- always written explicitly, never left to an assumed hardware reset default, since the same real device is now initialized a second time mid-boot, once with loopback on for Chapter 27's own unchanged demo, once with it off for this chapter's own new ARP exchange), verified via a real TCR register readback showing exactly the expected `0x60000` difference between the two modes. `028_kmain.c`'s own new demo appends after Chapter 27's own real multi-frame demo (kept completely unchanged, still loopback-mode): sends one real ARP request for the real gateway, receives and parses one real reply within a bounded `max_attempts` real-packet budget (needed since this is the first RTL8139 chapter where the device is not in loopback mode, so genuinely unrelated real traffic could arrive), and resolved the real gateway's MAC (`52:55:0a:00:02:02`) over exactly one real IRQ 11 delivery -- worked first boot, no bugs found. Verified two independent ways outside the kernel's own self-report: QEMU's own monitor `info pic` (confirming interrupt-mask state byte-for-byte unchanged from Chapter 27), and, new this chapter, QEMU's own monitor `xp` reading the received reply's own raw physical memory directly -- every field (destination/source MAC, EtherType, ARP header, sender/target MAC/IP) confirmed byte-for-byte against the kernel's own printed claim, the same independent technique Chapter 25 first used. A real, same-boot matched serial-log/VGA-screenshot pair confirms the identical text on the emulated VGA console. 64 real source files: 2 new (`028_arp.h`/`028_arp.c`), 3 changed (`028_rtl8139.h`/`028_rtl8139.c` gained the `enable_loopback` parameter and explicit TCR readback, `028_kmain.c` gained the ARP demo), 59 carried forward unchanged from Chapter 27.

Chapter 29 (DONE, commit 986d7bd): "A Real ARP Cache: Completing RFC 826's Own Merge_flag Logic" -- scope confirmed via two AskUserQuestion rounds: ARP cache (topic); the user's own explicit answer to the split question was "all the above" -- a real fixed-size translation table, real timeout-based expiry, and real LRU eviction, all together, deliberately broader than this book's usual single-mechanism chapter scope. New `029_arp_cache.h`/`029_arp_cache.c` complete the real Merge_flag logic RFC 826's own "Packet Reception" algorithm states but only half-specifies for a bounded table: the merge/insert half is cited directly from RFC 826's own quoted algorithm text, while RFC 826 itself explicitly states cache aging/timeout is "outside the scope of this protocol" -- that real gap is closed by RFC 1122 Section 2.3.2.1's own MUST/SHOULD requirement language ("An implementation of the Address Resolution Protocol (ARP) ... MUST provide a mechanism to flush out-of-date cache entries"). OSDev Wiki's own ARP page was re-checked and confirmed silent on caching entirely. LRU eviction itself is this book's own engineering design choice, not mandated by any cited source, consistent with Chapter 27's own precedent (round-robin transmit was likewise the book's own choice layered on cited register mechanics); each cache entry's single `last_used_tick` field honestly does double duty for both real elapsed-time expiry and LRU recency ordering, a real simplification against production stacks (e.g. Linux's neighbour subsystem tracks these separately) stated plainly in the code's own top-of-file comment rather than left implicit. Two genuine bugs were found and fixed via real QEMU `filter-dump` packet captures (a new diagnostic technique this chapter, used only for debugging, not as citable evidence): (1) this chapter's own cache demo was the first to call `arp_send_request()` more than once per boot, triggering Chapter 27's own already-documented single-transmit-descriptor-retrigger hang for the first time in practice -- fixed by routing `arp_send_request()` through Chapter 27's own already-established `rtl8139_send_queue()`/`rtl8139_wait_descriptor_sent()` round-robin path instead of the single-descriptor `rtl8139_send()`; (2) the demo's originally-planned three-host design (gateway/DNS/SMB, all three addresses per QEMU's own official networking documentation) hung resolving the SMB address 10.0.2.4 -- a real packet capture confirmed a genuine ARP request sent with zero replies ever received, proving 10.0.2.4 does not actually answer ARP in this exact QEMU 8.2.2 environment despite being documented -- fixed by redesigning around the two empirically-confirmed-responsive hosts (gateway 10.0.2.2, DNS 10.0.2.3) and reducing `ARP_CACHE_MAX_ENTRIES` from the originally-planned 2 to 1. `029_kmain.c`'s own new demo, appended after Chapter 28's carried-forward ARP exchange (itself needing the one real fix above -- not fully unchanged after all), runs a real 6-step sequence entirely over genuine network exchanges: Resolve #1 (gateway, miss, real exchange, insert), #2 (gateway again, real cache HIT, 0 IRQ 11 deliveries), #3 (dns, miss, real cache-full eviction of the gateway entry), #4 (gateway again, miss, confirming the real eviction), #5 (gateway again, real cache HIT, baseline for the expiry test), then a real `hlt`-driven wait of 301 real PIT ticks past the real 300-tick timeout, and #6 (gateway, miss, confirming real time-based expiry independent of LRU eviction) -- every step behaved exactly as predicted on the first successful boot after both fixes, zero "BUG" markers anywhere in the serial log. Verified two independent ways outside the kernel's own self-report: QEMU's own monitor `info pic` (confirming interrupt-mask state byte-for-byte unchanged since Chapter 26), and, new this chapter, QEMU's own monitor `xp` reading `g_cache`'s own real linked address directly (`0x1127a0` in `.bss`, found via `nm build/kernel.bin | grep g_cache`) -- all 20 raw bytes decoded by hand against the struct layout, matching the kernel's own printed claim after Resolve #6 exactly. A real, same-boot matched serial-log/VGA-screenshot pair confirms the identical text on the emulated VGA console. 66 real source files: 2 new (`029_arp_cache.h`/`029_arp_cache.c`), 3 changed (`029_arp.h`/`029_arp.c` gained the round-robin-send fix, `029_kmain.c` gained the cache demo), 61 carried forward unchanged from Chapter 28.

Chapter 30 (DONE, commit 3f873ce): "A Real Fedwire-Style Wire Transfer: AES-128, SHA-256, and HMAC From Scratch" -- scope confirmed via two AskUserQuestion rounds: topic (four options offered: ARP server behavior, a minimal IP layer, dynamic IDT-gate installation, or the Fedwire-style payment case study already flagged mid-Chapter-29) -- the user's own explicit answer was "Do #4 first then #1 after", making this chapter the Fedwire case study and queuing ARP server behavior as Chapter 31; then a second round narrowed the case study itself: message scope -- user chose "Core fields plus a real field-tag/delimiter format" over a bare core-fields-only option; crypto technique -- user chose "Both: encrypt-then-MAC" (real AES-128-CBC confidentiality plus real HMAC-SHA256 integrity, computed in that order) over either mechanism alone, directly satisfying the user's own earlier mid-Chapter-29 request to work in real encryption techniques. Three real cryptographic primitives implemented entirely from scratch: `030_aes.h`/`030_aes.c` (real AES-128, cited field-for-field from NIST FIPS 197 -- Nk=4/Nb=4/Nr=10, the real S-box/inverse S-box, ShiftRows/MixColumns and their inverses, key expansion with the real Rcon values, CBC chaining cited separately from NIST SP 800-38A); `030_sha256.h`/`030_sha256.c` (real SHA-256, cited field-for-field from NIST FIPS 180-4); `030_hmac.h`/`030_hmac.c` (real HMAC, cited from RFC 2104, re-fetched to confirm its own exact key-handling wording). Every one of the three was proven correct against a real published known-answer test vector via a native (non-freestanding) build before ever running in the kernel -- a new discipline this chapter introduced specifically because hand-rolled cryptography can be wrong in a way that is still perfectly self-consistent. Two real problems were caught this way before reaching the kernel: a WebFetch extraction of FIPS 197's own S-box table came back column-major/transposed -- a PDF-table-extraction artifact, caught by recognizing the extracted "row 0" matched this book's own canonical S-box's column 0, then proven correct by matching FIPS 197's own Appendix B test vector end-to-end; and a transcription bug in this chapter's own hardcoded SHA-256 test-vector array (not the implementation), caught and fixed by recomputing the expected bytes directly in Python. `030_fedwire.h`/`030_fedwire.c` build and parse a real tag-delimited wire-transfer message, cited field-for-field from two independently-corroborating real sources (a studylib.net mirror of Fedwire Funds Service's own real historical "Format Reference Guide", and Oracle FLEXCUBE's own real "FEDWIRE Interface" technical manual, which quotes the identical tag numbers/widths from its own real sample message), with Federal Register notice 2018-14351 cited honestly for the real historical context that this is the classic, now-superseded (as of a real July 2025 migration to ISO 20022) format. Every bank name, ABA routing number, account identifier, and person/company name in the demo is entirely fictional, invented for this book, never presented as functional payment infrastructure, with real Fedwire's own actual non-public security protocol explicitly stated as out of scope -- this chapter's own AES/HMAC construction is framed honestly as a general-purpose illustrative pattern, not a reproduction of it. This chapter's own first real kernel build failed to link (`__umoddi3`/`__udivdi3`, undefined): `amount_cents` was originally a `uint64_t` (matching the real 12-digit Fedwire Amount field width), and converting it to/from decimal digits needed 64-bit division this freestanding kernel has never linked libgcc for -- root-caused immediately as the exact same class of failure Chapter 7 already hit and documented, fixed the same way (scoped to `uint32_t`, the real limitation stated honestly in a comment rather than hidden). `030_kmain.c`'s own new demo, appended after Chapter 29's carried-forward ARP cache demo (kept completely unchanged), builds the fictional message, PKCS#7-pads it (RFC 5652 6.3), encrypts it with real AES-128-CBC, computes a real HMAC-SHA256 tag over the ciphertext (encrypt-then-MAC), sends the real 288-byte encrypted+authenticated frame over the same real RTL8139 hardware loopback path this book has used since Chapter 27, verifies the real HMAC before attempting any decryption, and recovers every field exactly -- then repeats with one ciphertext byte deliberately flipped, proving the real HMAC genuinely catches tampering rather than merely claiming to (verification correctly failed, message correctly refused before any decryption was attempted). Worked first real boot, zero "BUG" markers anywhere in the serial log. Verified two independent ways outside the kernel's own self-report: QEMU's own monitor `info pic` (confirming interrupt-mask state byte-for-byte unchanged since Chapter 26), and, new this chapter, an independent Python cross-check -- extracting the kernel's own printed padded-plaintext/ciphertext/tag hex dumps from the serial log and independently recomputing real AES-128-CBC (via the real, independent `pycryptodome` library) and real HMAC-SHA256 (via Python's own real stdlib `hashlib`/`hmac`) with the same known fixed demo keys, entirely outside this kernel's own `030_aes.c`/`030_sha256.c`/`030_hmac.c` -- confirming exact byte-for-byte agreement, directly extending Chapter 11's own established independent-cross-check precedent into the crypto domain. A real, same-boot matched serial-log/VGA-screenshot pair (visually confirmed) shows the identical text on the emulated VGA console. 68 real source files: 4 new pairs (`030_aes.h`/`.c`, `030_sha256.h`/`.c`, `030_hmac.h`/`.c`, `030_fedwire.h`/`.c`), 1 changed (`030_kmain.c` gained the demo), 60 carried forward unchanged from Chapter 29.

Chapter 31 (DONE, commit 7cf7d1b): "A Real ARP Server: Answering Requests About This Kernel's Own Address" -- scope confirmed via two AskUserQuestion rounds: whether the server should also opportunistically cache the requester's own IP/MAC into Chapter 29's real ARP cache -- the user's own explicit answer was "Minimal: reply only", so this chapter deliberately implements only RFC 826's own reply branch, not its coupled Merge_flag/table-insert half; and whether the demo should also prove the real refusal/non-reply case, not just the positive reply case -- the user's own explicit answer was "Yes, add the refusal/non-reply proof". New `031_arp_server.h`/`031_arp_server.c`: `arp_server_handle_frame()` examines one real received frame and, cited field-for-field from RFC 826's own full "Packet Reception" algorithm's reply branch (re-fetched to confirm exact wording), builds and sends a real reply -- swapping hardware/protocol fields, setting the opcode to REPLY, unicasting back to the real requester's own hardware address -- when the request's own target protocol address matches this kernel's own IP, and refuses outright (no partial or best-effort reply) for any other target IP, closing the exact real gap `031_arp.h`'s own top-of-file comment (unchanged since Chapter 28) already named: "this kernel never answers an incoming ARP request asking about its own address, only ever asks about someone else's." `031_kmain.c`'s own new demo, appended after Chapter 30's carried-forward Fedwire demo (kept completely unchanged), needed a genuinely new technique: since no real external host on this exact QEMU segment would ever organically ask this kernel's own IP anything, a new `build_arp_request_frame()` helper (mirroring `031_arp.c`'s own `arp_send_request()` but parameterized on an arbitrary sender) constructs a synthetic real request as if asked by a fictitious neighbor host, routed over real hardware loopback straight back to this same device's own receiver -- extending Chapter 30's own synthetic-frame-over-loopback precedent to a case with no cooperating asker at all. Part 1 proves the real positive case: a synthetic request for this kernel's own IP gets a real reply, independently verified field-by-field by hand against RFC 826's own quoted branch. Part 2 proves the user's own explicitly requested real refusal case, but only after a genuine mid-chapter correction: the first attempt polled `rtl8139_receive_next_packet()` in a bounded loop to confirm no reply ever arrived, and hung on boot -- reading `031_rtl8139.c` line by line showed that function is a genuinely BLOCKING real wait (`for (;;) { ...; hlt; }` against a real packet's own length header) with no code path that ever returns 0, exactly wrong for proving a negative on a ring expected to stay empty. The real fix reads this driver's own already-exposed `rtl8139_get_rx_offset()` directly instead -- before `arp_server_handle_frame()` runs, immediately after, and again after a real, bounded 100-tick (one real second) wait -- confirming the real ring read-position never advances, proving no reply was ever sent without ever risking an unbounded `hlt`. Worked first real boot after that fix, zero "BUG" markers anywhere in the serial log: Part 1's own real reply said "10.0.2.15 is at 52:54:0:12:34:56", correctly matching this kernel's own real MAC/IP; Part 2's own real refusal left the ring's read-position at a constant 204 across all three checks. Verified independently outside the kernel's own self-report via QEMU's own monitor `info pic` (confirming interrupt-mask state byte-for-byte unchanged since Chapter 26), plus a matched same-boot serial-log/VGA-screenshot pair. 74 real source files: 2 new (`031_arp_server.h`/`031_arp_server.c`), 1 changed (`031_kmain.c` gained the demo and a new `build_arp_request_frame()` helper), 71 carried forward unchanged from Chapter 30.

Chapter 32 (DONE, commit e9a1ac9): "A Real NACHA ACH Batch: P2P Group-Expense Splitting" -- first of the 5 queued financial-services case-study chapters. Scope confirmed via three AskUserQuestion rounds: core feature -- user chose "Transfer + group splitting" (a real single-sender-to-single-receiver transfer plus real group expense splitting, over a bare instant-transfer-only option); message format -- user chose "NACHA ACH file format" (the real, well-documented US ACH batch/entry-record format) over an invented tag format; crypto technique -- user chose "Reuse Chapter 30's AES+HMAC" (no new cryptographic primitive, keeping this chapter's own scope on the P2P/message-format/splitting logic itself). New `032_ach.h`/`032_ach.c`: a real, cited, fixed-width 94-byte-per-record NACHA ACH encoder/decoder -- File Header, Batch Header, Entry Detail, Batch Control, File Control -- cited field-for-field from two independently-corroborating real bank technical PDFs (gbankmo.com's own "ACH File Format Specification", independent-bank.com's own "NACHA File Layout Guide") plus Nacha's own official developer guide (achdevguide.nacha.org) for real transaction codes, the real general alphanumeric/numeric field-formatting rule, and the real Trace Number definition; the real Service Class Codes corroborated by a real Bank of America NACHA specification; the real WEB Standard Entry Class Code (consumer-authorized internet-initiated debits) cited via vericheck.com quoting Nacha's own rule text. Implements the real blocking-factor-10 padding rule (real Dynamics 365 explainer), the real Entry Hash algorithm (sum of 8-digit routing numbers truncated to 10 digits, cited from the first bank PDF and corroborated by achgenie.com), and the real ABA routing-number check-digit algorithm (weights 3/7/1 repeating, cited from Apache Commons Validator's own reference docs, corroborated by paytia.com). Three honesty notes stated explicitly rather than silently assumed: "group expense splitting" invents no NACHA mechanism -- it is simply N real Entry Detail records sharing one real batch, exactly how a real payroll batch already works, with only the scenario (a real Company Entry Description, "DINNERSPLT", exactly its cited 10-character width) layered on top, mirroring Chapter 29's own LRU-eviction honesty precedent; the real Trace Number's own 8-digit-routing-plus-7-digit-sequence composition is this chapter's own common-convention choice, since Nacha's own official text states only that it must be ODFI-assigned, ascending, and unique per entry, not that exact substructure; and the real Batch Control Record's own legacy 19-byte Message Authentication Code field is left as real spaces rather than repurposed, with this chapter's own AES-128-CBC+HMAC-SHA256 protection (reused completely unchanged from Chapter 30, per this chapter's own confirmed scope) wrapping the whole real ACH file byte stream instead. `ach_parse_file()` follows `032_fedwire.c`'s own already-established `fedwire_parse_message()` precedent: validates real record-type-code structure before writing any field, refusing outright (returning 0) on the first structurally invalid record or malformed numeric digit. `032_kmain.c`'s own new demo, appended after Chapter 31's carried-forward ARP server demo (kept completely unchanged), builds a real 3-participant NACHA batch splitting a fictional $75.25 dinner bill (`$25.00`/`$30.50`/`$19.75`), PKCS#7-pads and real AES-128-CBC-encrypts the real 940-byte file, computes a real HMAC-SHA256 tag over the ciphertext (encrypt-then-MAC), and sends the real 992-byte frame over the same real RTL8139 hardware loopback path this book has used since Chapter 27, tagged with a new real EtherType `0x88B6` (distinct from Chapter 30's own `0x88B5`, both within the real IEEE 802 vendor-reserved range cited via RFC 5342 Appendix B.2) -- verifies the real HMAC before any decryption, recovers every field via `ach_parse_file()`, and repeats with one ciphertext byte deliberately flipped to prove real tamper detection. One honest mid-chapter fix, to the demo's own verification logic rather than to the real ACH format itself: the real on-disk `individual_name` field is genuinely space-padded (the real NACHA alphanumeric rule), while this chapter's own in-memory original was only ever filled up to its real string length and left zero-padded by `zero_bytes()` -- comparing the two padding conventions byte-for-byte falsely reported a mismatch even though the real round trip was correct; fixed to compare the real name content, then separately confirm the rest of the real field is genuinely all spaces. Worked first real boot after that fix, zero "BUG" markers anywhere in the serial log. Verified two independent ways outside the kernel's own self-report: QEMU's own monitor `info pic` (confirming interrupt-mask state byte-for-byte unchanged since Chapter 26), and a from-scratch, non-kernel Python re-derivation of every real NACHA field -- record types, real ABA check digits (independently recomputed via the same real weighted formula), the real Entry Hash, and the real per-participant/batch dollar totals -- reading only the kernel's own printed hex dump from the serial log and sharing no code with `032_ach.c`, matching byte-for-byte, directly extending Chapter 11's own established independent-cross-check precedent. A real, same-boot matched serial-log/VGA-screenshot pair (visually confirmed) shows the identical text on the emulated VGA console. 78 real source files: 2 new (`032_ach.h`/`.c`), 1 changed (`032_kmain.c` gained the demo), 75 carried forward unchanged from Chapter 31.

Chapter 33 (DONE, commit e44d382): "A Real 'Pay in 4' BNPL Checkout: Regulation Z APR and an ISO 8583 Authorization" -- second of the 5 queued financial-services case-study chapters (the BNPL / short-term lending app). Scope confirmed via one AskUserQuestion round of three questions: core feature -- user chose "Installment schedule + APR" (a Pay-in-4 schedule plus the real Truth in Lending disclosures -- amount financed, finance charge, total of payments, payment schedule, and an APR by Regulation Z Appendix J's actuarial method, all in integer cents) over "Schedule + credit check" and "Schedule + ACH collection"; message format -- user chose "ISO 8583 auth message" (MTI + bitmap + data elements) over reusing NACHA or an invented tag format; crypto -- user chose "Reuse Ch 30 AES+HMAC" over adding HKDF. New `033_bnpl.h`/`.c`: a Pay-in-4 plan builder whose central modeling point is that installment 1, paid at checkout/consummation, is a 1026.18 DOWNPAYMENT (subtracted to get the amount financed, excluded from the total of payments), leaving three regular two-week repayments for Appendix J's equation (26 unit-periods a year, a 2-week interval being "a multiple of a week"); a 1026.2(a)(17) coverage test (finance charge, or more than four installments not counting the downpayment); and an integer-only APR solver that bisects over the discount factor v = 1/(1+i) in Q30 rather than over i, so every Horner partial sum stays bounded by the total of payments and nothing overflows 64 bits (amounts capped below 2^24 cents, at most 12 payments -- stated limits, refused not truncated), with its own restoring shift-and-subtract `udiv64_32()` for the one 64-by-32-bit division instead of libgcc's `__udivdi3` (the Chapter 7/30 link failure, avoided rather than hit). Rounding convention (leftover cents on installment 1; fee split over installments 2-4) and plan shape stated as this book's own choices. Citation honesty note, new this chapter: this sandbox's network egress policy blocked every direct fetch of consumerfinance.gov, ecfr.gov, govinfo.gov, and law.cornell.edu (explicit "blocked by the network egress proxy" errors), so Regulation Z wording (1026.2(a)(17), 1026.18, 1026.22(a)(2)'s 1/8-point tolerance, Appendix J's unit-period/APR definitions) was taken from web-search results surfacing those official pages, stated plainly in `033_bnpl.h` and the chapter; the May 2024 CFPB BNPL interpretive rule and its May 2025 withdrawal are cited for context only. New `033_iso8583.h`/`.c`: an ISO 8583:1987 ASCII encoder/decoder (DEs 2, 3, 4, 7, 11, 12, 13, 38, 39, 41, 42, 48, 49), cited from source code read in full -- a clone of moov-io/iso8583 (Go, `specs/spec87ascii.go`, commit 5219813) and pyiso8583 4.0.1 (PyPI, `default_ascii`) -- plus a Luhn check digit (ISO/IEC 7812-1 via search results); refuses outright on unknown DEs, bit 1 (secondary bitmap), non-digit numerics, over-length fields, DE 4 amounts above uint32, truncation, or trailing bytes. DE 48's 126-byte plan payload layout is stated as this book's own (DE 48 is private-use). Native proof before the kernel: `gcc -m32` could not build a hosted test here (no 32-bit libc), so known-answer tests ran as a 64-bit hosted build of the unchanged fixed-width code ($1,000 at 1%/month x 12 = $88.85 -> 12.00%; zero fee -> 0.00%; the demo plan -> 51.67%; negative-finance-charge refusal; leap-year date; DE 48 round trip), plus 1000 randomized loans against numpy_financial's IRR (worst difference 0.004994 points -- only the final round-to-hundredths). `033_kmain.c` gains `bnpl_demo()` (static buffers, 16 KiB boot stack; explicit byte-loop struct copy to avoid a gcc-emitted memcpy call), appended after Chapter 32's carried-forward demo: Part 1 prints Plan A ($199.99, no fee: $50.02 at checkout + 3 x $49.99, APR 0.00%, NOT Reg-Z-covered) and Plan B (same purchase, flat $6.00 fee: 3 x $51.99, amount financed $149.97, finance charge $6.00, total of payments $155.97, APR 51.67%, covered); Part 2 a fictional terminal sends a 0100 authorization request (fictional Luhn-valid PAN 9999990033000015, DE 48 "P4") sealed with Chapter 30's AES-128-CBC + HMAC-SHA256 under new EtherType 0x88B7; the issuer verifies HMAC before decrypting, checks Luhn, prices Plan B, and answers with a 0110 (DE 38 "FIC033", DE 39 "00", DE 48 plan) that the terminal verifies, decodes, and matches to Plan B exactly; a one-byte-flipped copy of the response frame is refused before decryption. Worked first real boot, zero "BUG" markers in the 574-line serial log. One real finding, from the outside check rather than the kernel: moov-io's Spec87ASCII declares its bitmap with Length 16 counted in DECODED bytes, so it always writes a 128-bit (primary+secondary, 32 hex chars) bitmap even with bit 1 clear, and refused the kernel's 8-byte-primary-bitmap messages at DE 42; pyiso8583 and moov-io's own docs/bitmap.md use the 8-byte primary layout this kernel uses -- `033_iso8583.h`'s citation (which had wrongly claimed the two libraries agree on the bitmap) was corrected, kernel code unchanged. Verified independently: a Python script (pyiso8583 decode of both messages, Luhn, schedule, `datetime` due dates, every 1026.18 disclosure, numpy_financial APR, 1/8-point tolerance -- all 30 checks OK); a Go program using moov-io's own field table with its default 8-byte bitmap (Go 1.26.0 toolchain), which unpacks both messages and re-packs them byte-for-byte identical to the kernel's; QEMU `info pic` (pic0 imr=f8 / pic1 imr=f7, unchanged since Chapter 26); and a same-boot VGA `screendump -f png` matching the serial log. Toolchain for this session installed via apt (nasm, qemu-system-x86, grub-pc-bin, xorriso, mtools); Chapter 32's ISO was first rebuilt and booted as a pipeline check and matched its recorded output (same 2556-sector ISO, same kernel end 0x11c298, same PIC masks). Kernel image grew to end at 0x121438 (16060 free frames, down from 16065) -- reported as observed. 82 real source files: 4 new (`033_bnpl.h`/`.c`, `033_iso8583.h`/`.c`), 1 changed (`033_kmain.c`), 77 carried forward unchanged from Chapter 32.

[stated] Mid-Chapter-32, asked for one more future case study: a video-streaming/media-delivery chapter mimicking a service like Netflix's real architecture (real HTTP range-request byte-serving, chunked/segmented delivery, an adaptive-bitrate manifest format -- not yet scoped in detail). Confirmed via AskUserQuestion: queue it AFTER all 5 financial-services chapters, not inserted earlier into that already-confirmed order.

Chapter 34 (DONE, commit 9c0773a): "An Insurance Comparison App: Quote Aggregation Across Carriers, in ACORD XML" -- third of the 5 queued financial-services case-study chapters (insurance comparison & claims assistant). Scope confirmed via one AskUserQuestion round of three questions: core feature -- user chose "Quote aggregation across carriers" over "Claims filing/tracking only" and "Both"; message format -- user chose "ACORD-style XML claim/application fields" over reusing Ch 33's ISO 8583 or an invented tag format; crypto -- user chose "Reuse Ch 30 AES+HMAC" over adding HKDF. New `034_insurance.h`/`.c`: a real multiplicative rating engine (base rate x a chain of independently-justified factors -- age, territory tier, at-fault-accident count, vehicle value/age, collision deductible -- all in basis points/10000ths), cited at the level of the whole rating-table SHAPE (NAIC's own general ratemaking material, via search results since content.naic.org was blocked: "classification rates may be modified ... in accordance with rating plans which establish standards for measuring variations in hazards", and its own WC material's "premiums are calculated as a base rate multiplied by payroll"; the real 100/300/100 split-limit liability convention likewise cited via search only) with every SPECIFIC factor value and each of 3 fictional carriers' own base rate/factor table stated plainly as this book's own invention (Chapter 29/32 honesty precedent, applied at table level not field level). Integer-only arithmetic throughout (`apply_factor()`'s own round-half-up via its own restoring shift-and-subtract `udiv64_32()`, avoiding the Chapter 7/30/33 `__udivdi3` link failure), stated bounds (`INS_MAX_BASE_RATE_CENTS` 2^24, `INS_MAX_FACTOR` 2^16) refused not truncated. New `034_acord.h`/`.c`: a restricted ACORD-style XML encoder/decoder (no attributes, no same-name self-nesting) built from a real, UNMODIFIED GitHub sample this book read in FULL -- a clone of appulate/appulate-acordxml-svc-sample (`Testing/case1/case1.xml`, `Testing/case2/case2_success.xml`, real Workers' Comp submissions) -- since acord.org and every third-party ACORD-schema site (schemas.liquid-technologies.com, xml.coverpages.org, en.wikipedia.org) were blocked by the sandbox's egress policy (same honesty note as Chapter 33's Reg Z sites). Every element name is marked [OBSERVED] (read directly in that real file: ACORD, SignonRq, InsuranceSvcRq, ItemIdInfo/OtherIdentifier/UID, GeneralPartyInfo/NameInfo/PersonName/CommlName, Addr, Policy/LOBCd/ContractTerm, Coverage/CoverageCd/Limit/FormatCurrencyAmt/Amt/LimitAppliesToCd, Deductible, PolicyStatusCd, PolicyAmt -- this last one observed labeling a PRIOR policy's premium, reused here for a freshly quoted one, a stated reuse not a verbatim citation), [WEAK] (real per search results only, not read in full: root names PersAutoPolicyQuoteInqRq/...Rs, LOBCd value AUTOP, CoverageCd values BI/PD/COLL), or [INVENTED] (PersDriverInfo/PersVehInfo and children, TerritoryTierCd -- each still carrying amounts through the real observed FormatCurrencyAmt/Amt leaf pattern). `acord_parse_request()`/`acord_parse_response()` follow the established refusal discipline (missing element, non-digit numeric, wrong LOBCd/CoverageCd/LimitAppliesToCd/PolicyStatusCd value, or trailing bytes all refused outright). A real bug found only by BOOTING, not by the native test: `make_wrapped_tag()`'s scratch buffers reused `034_acord.h`'s own `ACORD_MAX_NAME_LEN` (24, sized for surname/given-name fields) for tag-name buffers too, and two real tag names (`PersAutoPolicyQuoteInqRq`/`...Rs`) are exactly 24 characters -- one byte short for a CLOSING tag's own `strlen+4` requirement -- causing a real one-byte stack overflow that the native test's own stack layout didn't disturb but that crashed the kernel with a genuine `*** CPU EXCEPTION: Page Fault ***` at a corrupted address; fixed with a dedicated `ACORD_MAX_TAG_LEN` (32) used only for tag-wrapping buffers, kept and stated as this chapter's own explicit lesson on what native testing does and doesn't prove. `034_kmain.c` gains `insurance_demo()` (mirroring Ch 33's two-role one-machine pattern: a fictional comparison app and a fictional carrier aggregator), appended after Chapter 33's carried-forward demo: a fictional applicant (age 29, 11 years licensed, 1 at-fault accident, territory tier 2, $18,500/4-year-old vehicle, requesting 100/300/100 split-limit liability + $500 collision deductible) is quoted against 3 fictional carriers (FictCasualty/FictMutual/FictGuard) sealed with Chapter 30's AES-128-CBC + HMAC-SHA256 under new EtherType 0x88B8; ranked cheapest-first ($658.13/$682.74/$741.38), round-tripped through the ACORD response, matched exactly; a one-byte-flipped copy of the response frame refused before decryption. A second real, harmless bug found and fixed: this kernel's own hand-rolled `kprintf()` has no field-width support at all (`%-14s` printed literally rather than column-aligning), fixed with a small hand-written `print_padded()` instead of asking printf to do something it was never written to do. Worked (after both fixes) on the fixed build's first boot, zero "BUG" markers in the 606-line serial log. Verified independently: a Python script using the standard-library `xml.etree.ElementTree` (a real, independent XML parser, not 034_acord.c's own scanner) to decode both messages, plus a from-scratch re-derivation of every rating factor and the ranked premiums (18/18 checks OK) -- one real mismatch surfaced on the first run (a `.5`-cent rounding boundary: Python's `round()` is round-half-to-EVEN, the kernel's own documented convention is round-half-UP), fixed in the VERIFICATION SCRIPT (adding an explicit `round_half_up()`), not in the kernel, which was correct throughout; QEMU `info pic` (pic0 imr=f8 / pic1 imr=f7, unchanged since Chapter 26); and a same-boot VGA `screendump -f png` matching the serial log. 84 real source files: 4 new (`034_insurance.h`/`.c`, `034_acord.h`/`.c`), 1 changed (`034_kmain.c`), 79 carried forward unchanged from Chapter 33.

Chapter 35 (DONE, commit 9590c45): "A Micro-Investing / Robo-Advisor App: Round-Up Investing, Real FIX 4.4 Orders" -- fourth of the 5 queued financial-services case-study chapters. Scope confirmed via one AskUserQuestion round of three questions: core feature -- user chose "Round-up + risk-based allocation" over "Round-up investing only" and "Risk-based allocation + scheduled rebalancing"; message format -- user chose "FIX protocol NewOrderSingle" over reusing Ch 34's ACORD XML or an invented tag format; crypto -- user chose "Reuse Ch 30 AES+HMAC" over adding HKDF. New `035_investing.h`/`.c`: real round-up mechanic (round a purchase up to the next dollar, spare change is the pool) plus a real 3-band risk-questionnaire allocation model (CONSERVATIVE/MODERATE/AGGRESSIVE, each with a real-shaped target bond/stock/cash split), cited at the model-SHAPE level only (real, general micro-investing/robo-advisor pattern; every specific fund, NAV, percentage split, and scoring formula stated plainly as this book's own invention, the Chapter 29/32/34 honesty precedent). 3 fictional funds using the real US mutual-fund "X"-suffix ticker convention (FBNDX $10.15, FSTKX $42.87, FCSHX $1.00 -- the last using the real standard stable cash-fund NAV). Integer-only fixed-point throughout: allocation in round-half-up cents (last fund absorbs the leftover, guaranteeing an exact sum, the Ch32/33 leftover-cents convention), share quantities in MILLI-SHARES (thousandths of a share) via the module's own self-contained restoring shift-and-subtract `udiv64_32()`, avoiding the Chapter 7/30/33/34 `__udivdi3` link failure. New `035_fix.h`/`.c`: a real FIX 4.4 tag=value message encoder/decoder (NewOrderSingle MsgType D, ExecutionReport MsgType 8), cited from source code read in FULL -- a clone of quickfix/quickfix's own `spec/FIX44.xml`, the real data dictionary that engine's own generator builds from -- since fixtrading.org itself was blocked by the sandbox's egress policy (same honesty note as Ch 33/34's own blocked standards sites). Every tag number (8/9/35/49/56/34/52/10 header/trailer; 11/1/21/55/54/60/40/38/59 NewOrderSingle; 37/17/150/39/151/14/6/32/31 ExecutionReport) and enum value (Side=1 BUY, OrdType=1 MARKET, TimeInForce=0 DAY, HandlInst=1, ExecType=F TRADE, OrdStatus=2 FILLED) read directly from that real file. Real FIX SOH (0x01) delimiters on the wire; this chapter's own demo substitutes a printable '|' only when PRINTING to the serial log, stated explicitly as display-only. `fix_parse_message()` computes/verifies a real BodyLength and CheckSum, refusing on any mismatch, malformed field, or trailing bytes -- the established refusal discipline. `035_kmain.c` gains `investing_demo()` (mirroring the established two-role one-machine pattern: a fictional app and a fictional brokerage), appended after Chapter 34's carried-forward demo: 4 fictional purchases ($4.37/$12.05/$7.50/$19.99) round up to a $2.09 pool; a 3-answer risk questionnaire (40/60/50) scores 50 -> MODERATE; the pool splits $0.84/$1.05/$0.20 across FBNDX/FSTKX/FCSHX -> 0.083/0.024/0.200 shares; each becomes a real FIX NewOrderSingle sealed with Chapter 30's AES-128-CBC + HMAC-SHA256 under new EtherType 0x88B9, filled by the brokerage with a real FIX ExecutionReport (OrdStatus FILLED); a one-byte-flipped copy of the last execution report refused before decryption. TWO real bugs found, both stated as explicit lessons: (1) a genuinely significant, 34-chapters-latent kernel bug this chapter's own larger image size exposed for the first time -- the Multiboot2 INFORMATION STRUCTURE itself (as opposed to the boot MODULE Chapter 17 already reserves) was never reserved in the physical memory allocator, so a later frame allocation could silently overwrite the very bytes `elf_load()` needed, producing two DIFFERENT failure modes (total silence once, then a printed `elf_load: module too small` + garbage e_entry `0xf000ff53` the next boot) from the identical kernel image -- itself the clue pointing at memory corruption rather than ordinary logic error; fixed with a new `multiboot_total_size()` (035_multiboot.h/.c, reading the real `total_size` field already cited in `multiboot_find_mmap()`'s own comment) and one new `pmm_reserve_range(mboot_info_addr, mboot_info_addr + multiboot_total_size(...))` call right after `pmm_init()`, mirroring Chapter 17's own module-reservation precedent exactly; verified fixed by 3 consecutive identical, correct, zero-BUG boots of the same ISO. (2) a small, harmless bug in this chapter's own new `price_to_string()`: its cursor was never advanced past a single whole-dollar digit before writing the decimal point, printing FCSHX's real $1.00 NAV as "$.00" -- fixed to match `035_investing.c`'s own correct `put_digits()` pattern. Verified independently: a Python script using `simplefix` (a real, independent, published PyPI FIX parser, not `035_fix.c`'s own codec) to decode all 6 FIX messages, plus a from-scratch re-derivation of the round-up pool, risk band, allocation, and every milli-share quantity (25/25 checks OK); QEMU `info pic` (pic0 imr=f8 / pic1 imr=f7, unchanged since Chapter 26); and a same-boot VGA `screendump -f png` matching the serial log. 86 real source files: 4 new (`035_investing.h`/`.c`, `035_fix.h`/`.c`), 2 changed (`035_kmain.c`, and `035_multiboot.h`/`.c` for the reservation fix -- counted once as the fix touches a carried-forward file), 82 carried forward unchanged from Chapter 34 (net of the multiboot fix).

Chapter 36 (DONE, commit PENDING): "A Personal Budgeting / Cash-Flow Tracker: Real OFX, Automatic Categorization, and a Cash-Flow-Gap Forecast" -- fifth and LAST of the 5 queued financial-services case-study chapters. Scope confirmed via one AskUserQuestion round of three questions: core feature -- user chose "Auto-categorization + cash-flow-gap forecasting" over categorization-only or forecasting-only; message format -- user chose "OFX (Open Financial Exchange)" over reusing Ch 33's ISO 8583 or an invented tag format; crypto -- user chose "Reuse Ch 30 AES+HMAC" over adding HKDF. New `036_ofx.h`/`.c`: a real OFX 1.02 bank-statement-download encoder/decoder, cited from source read in FULL -- a real, unmodified OFX 1.02 sample fetched from the open-source Python library ofxparse (github.com/jseutter/ofxparse, `tests/fixtures/checking.ofx`), since ofx.net itself was blocked by the sandbox's egress policy (same honesty note as Ch 33/34/35's own blocked standards sites). That real fetched file settled a structural fact this chapter builds around: OFX 1.x is SGML, not XML -- an AGGREGATE element (STMTTRNRS, STMTRS, BANKACCTFROM, BANKTRANLIST, STMTTRN, LEDGERBAL, etc.) closes explicitly, but a LEAF element (CODE, TRNTYPE, DTPOSTED, TRNAMT, FITID, NAME, BALAMT, DTASOF, etc.) never does -- implicitly ended by whatever tag comes next. Every element name cited is marked [OBSERVED], read directly in that real file: the plain header block (OFXHEADER/DATA/VERSION/SECURITY/ENCODING/CHARSET/COMPRESSION/OLDFILEUID/NEWFILEUID), OFX/SIGNONMSGSRSV1/SONRS/STATUS/CODE/SEVERITY/DTSERVER/LANGUAGE, BANKMSGSRSV1/STMTTRNRS/TRNUID/STMTRS/CURDEF, BANKACCTFROM/BANKID/ACCTID/ACCTTYPE, BANKTRANLIST/DTSTART/DTEND, STMTTRN/TRNTYPE/DTPOSTED/TRNAMT/FITID/NAME, LEDGERBAL/BALAMT/DTASOF, and the real TRNTYPE values DEBIT/CREDIT and ACCTTYPE value CHECKING. `036_ofx.c`'s own encoder emits real unclosed leaf tags on the wire (matching the fetched sample exactly); its own `find_leaf()` reads a leaf's value by scanning to the next literal '<' (never hunting a matching close tag, since a genuine OFX leaf never has one), while `find_aggregate()` mirrors Ch 34's own restricted-schema real-XML approach for container elements -- which to do for which field is fixed by this chapter's own known schema, the same restricted-but-honest technique. Refuses outright on a wrong header block, wrong STATUS/CODE, a malformed date/amount, a DEBIT/CREDIT sign mismatch against its own TRNAMT, or trailing bytes. New `036_budget.h`/`.c`: keyword-based auto-categorization (COFFEE->Dining, GROCERY->Groceries, ELECTRIC/UTILITY->Utilities, RENT->Housing, PAYROLL/SALARY->Income) rolled into budget-vs-actual against fictional monthly limits, plus a day-by-day cash-flow-gap forecast projecting a starting balance forward against recurring income/expense items to find the first negative-balance day -- both real, general personal-finance-app techniques, cited at the model-SHAPE level only; every category name, keyword rule, budget limit, and recurring item stated plainly as this book's own invention (Ch 29/32/34/35 honesty precedent). `036_budget.h`'s own top comment states explicitly why this module, unlike Ch 33-35's own fixed-point modules, needs NO self-contained `udiv64_32()`: every intermediate value here (signed cents, day counts under 60, intervals under 30) fits a plain 32-bit `%`/`/` safely on its own, since nothing here multiplies two already-large quantities together first. `036_kmain.c` gains `budget_demo()` (mirroring the established two-role one-machine pattern: a fictional bank and a fictional budgeting app), appended after Chapter 35's carried-forward demo (which depends on Ch 35's own real Multiboot2-reservation fix remaining in place, confirmed unaffected here): a fictional bank builds a real OFX feed for one fictional checking account (5 transactions, a real bank-reported LEDGERBAL of $332.11 -- deliberately NOT recomputed from the shown transactions, since a real OFX ledger balance is the bank's own system-of-record figure) sealed with Chapter 30's AES-128-CBC + HMAC-SHA256 under new EtherType 0x88BA; the app verifies, decrypts, parses, categorizes all 5 transactions (all within their fictional monthly budgets), and forecasts a genuine cash-flow gap 5 days out (a $1,200 fictional rent debit due day 5 would take the $332.11 balance negative before a $2,500 fictional payroll deposit arrives on day 10); a one-byte-flipped copy of the OFX feed frame refused before decryption. Worked reliably across 3 consecutive identical, zero-BUG boots. ONE bug found, this chapter's own new code REPEATING a mistake Chapter 34 already made and fixed: `%-24s`/`%-14s`/`%-10s` field-width specifiers, unsupported by this kernel's own hand-rolled `kprintf()` (confirmed once again by reading `036_printf.c`'s own switch statement), printed literally instead of column-aligning; fixed with Ch 34's own `print_padded()`, carried forward unchanged -- reported plainly as a REPEATED lesson rather than silently corrected, since a second occurrence of an already-documented mistake is itself real, useful information about how the bug class recurs. Verified independently: a Python script using BeautifulSoup's `html.parser` (a real, independent, published HTML/SGML parser from PyPI -- and, not coincidentally, the same real technique the cited `ofxparse` library itself uses internally to handle OFX's own unclosed leaf tags, since a strict XML parser would refuse a real OFX file outright) to decode the real OFX message (using `.contents[0]`, not the naive `.text`, to correctly extract each leaf's own value despite html.parser's generic HTML nesting behavior), plus a from-scratch re-derivation of every category total, income, and the cash-flow-gap day (17/17 checks OK); QEMU `info pic` (pic0 imr=f8 / pic1 imr=f7, unchanged since Chapter 26); and a same-boot VGA `screendump -f png` matching the serial log. This chapter closes out all 5 originally-queued financial-services case studies (Ch 32-36). 88 real source files: 4 new (`036_ofx.h`/`.c`, `036_budget.h`/`.c`), 1 changed (`036_kmain.c`), 83 carried forward unchanged from Chapter 35.

NEXT: this book's chapters grow one at a time -- confirm scope with the user before starting the next chapter, the same pattern this author's other books use for ambiguous next steps. Chapter 36 completed the fifth and LAST of the 5 originally-queued financial-services case studies -- real OFX-fed automatic categorization plus a cash-flow-gap forecast, proven both inside the kernel and outside it (BeautifulSoup's html.parser + a from-scratch categorization/forecast recomputation), and honestly reported a REPEATED bug (the same unsupported printf field-width mistake Chapter 34 already made once) rather than silently fixing it. The video-streaming/media-delivery case study queues next (see the stated note above), followed by the remaining case studies in this exact order: an ATM system; POS (Point-of-Sale) and Smart Terminals; Billing & Payment Systems; flight ticket aggregation; sports ticket aggregation; betting systems; rental cars. [stated] After Chapter 33, the user asked for one more case study to be queued: an ATM system (real ISO 8583 withdrawal/balance-inquiry transactions, card/PIN handling, cash-dispense sequencing, receipt printing -- not yet scoped in detail), to be added to the queue AFTER the already-confirmed case studies above, i.e. after the video-streaming/media-delivery chapter. [stated] After the ATM system was queued, the user asked for one more case study, pasted from an external source rather than typed live: "POS (Point-of-Sale) and Smart Terminals" -- the lightweight, highly secure OS running on the hardware that interacts with a user's physical card at the edge of the network. The user's own pasted material (cited to unixfor.gr's "Business Applications" page and an NXP "Smarter World" blog post, "How to Achieve Secure Point of Sale (Linux)") frames it around two real themes to scope later: micro-kernel Linux as the common real-world OS choice for battery-powered mPOS (mobile point-of-sale) terminals and outdoor payment kiosks; and hardware-level security mapping onto Arm architecture -- cryptographic key storage, secure PIN pads, and isolated runtime environments. Queued as (g), after the ATM system. Updated full remaining case-study queue, in order: (b) insurance comparison & claims assistant; (c) micro-investing / robo-advisor; (d) personal budgeting / cash-flow tracker; (e) video-streaming/media-delivery; (f) ATM system; (g) POS (Point-of-Sale) and Smart Terminals. [stated] The user then asked for one more case study, again pasted from an external source (the same list "POS and Smart Terminals" came from, apparently a broader survey of Unix/Linux use in financial-services hardware): "Billing & Payment Systems" -- not yet scoped in any detail beyond that name; the user's own message named no specific sub-themes or citations for this one, unlike the ATM and POS entries above. Queued as (h), after POS/Smart Terminals. Updated full remaining case-study queue, in order: (b) insurance comparison & claims assistant; (c) micro-investing / robo-advisor; (d) personal budgeting / cash-flow tracker; (e) video-streaming/media-delivery; (f) ATM system; (g) POS (Point-of-Sale) and Smart Terminals; (h) Billing & Payment Systems. [stated] While mid-work on Chapter 35 (micro-investing/robo-advisor), the user asked for three more case studies queued: "flight tickets aggregation", "sports ticket aggregation", and "betting systems". None of the three has been scoped -- no real message format, crypto choice, or specific core feature has been confirmed via AskUserQuestion for any of them, and none of the earlier queue items (b)-(h) has been reordered ahead of them. Queued as (i), (j), (k), in that order, after Billing & Payment Systems. Updated full remaining case-study queue, in order: (b) insurance comparison & claims assistant; (c) micro-investing / robo-advisor; (d) personal budgeting / cash-flow tracker; (e) video-streaming/media-delivery; (f) ATM system; (g) POS (Point-of-Sale) and Smart Terminals; (h) Billing & Payment Systems; (i) flight ticket aggregation; (j) sports ticket aggregation; (k) betting systems. [stated] Immediately after, the user asked for one more: "rental cars" (car rental aggregation/booking, presumably, though not spelled out further -- not yet scoped). Queued as (l), after betting systems. Updated full remaining case-study queue, in order: (b) insurance comparison & claims assistant; (c) micro-investing / robo-advisor; (d) personal budgeting / cash-flow tracker; (e) video-streaming/media-delivery; (f) ATM system; (g) POS (Point-of-Sale) and Smart Terminals; (h) Billing & Payment Systems; (i) flight ticket aggregation; (j) sports ticket aggregation; (k) betting systems; (l) rental cars. A minimal IP layer and the still-open dynamic IDT-gate-installation topic (first named in Chapter 26) remain candidates after that queue. None of the remaining chapters has been scoped yet; scoping happens via AskUserQuestion when "next" reaches each one. Environment note for future sessions: if the official-regulation sites are still blocked by the sandbox's egress policy, say so in the chapter as Chapter 33 did, and prefer sources that can be read in full (open-source code on GitHub, PyPI packages) plus independent outside checks.
